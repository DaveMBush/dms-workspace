# Story 2.2: Remove Return of Capital handling

Status: ready-for-dev

## Story

As a developer,
I want every separate handling of "Return of Capital" removed and consolidated under Dividend,
So that the app models a single income concept per the PRD glossary and AD-7.

## Acceptance Criteria

1. `scripts/seed-postgres.ts` no longer seeds a `divDepositType` named "Return of Capital". The `seedDivDepositTypes` array contains exactly: Dividend, Interest, Capital Gains.
2. `docs/data-models.md` no longer lists "Return of Capital" as an example deposit type in the `divDepositType` model documentation (line 222 comment).
3. A repository-wide search (case-insensitive) for "return of capital" outside `_bmad-output/` returns zero matches.
4. The seed script still runs successfully via `tsx scripts/seed-postgres.ts` and seeds the remaining 3 types without error.
5. The server canonical seed (`get-top-div-deposit-types.function.ts`) is unchanged and still seeds exactly "Dividend" + "Deposit".
6. Story 2.1 unit tests pass: `nx test server` green, `scripts/seed-postgres.spec.ts` green. The `.skip()` markers that Story 2.1 placed on the red-phase assertions are removed as this story's first task (Task 1), so they run red before implementation and green after it.

## Tasks / Subtasks

- [ ] Task 1 (AC: #6): Unskip Story 2.1's tests and confirm the red phase.
  - [ ] 1.1 In `scripts/seed-postgres.spec.ts`, remove every `it.skip` marker that Story 2.1 added to the "Return of Capital" absence / exactly-three-types assertions (restore them to plain `it`). Do not modify any assertion, and do not touch the already-unskipped canonical-seed guard tests in `get-top-div-deposit-types.function.spec.ts`.
  - [ ] 1.2 Run the seed spec: confirm the newly-unskipped tests FAIL against the current seed (red phase — "Return of Capital" is still present). This is expected — proceed to Task 2 without "fixing" these failures by weakening them.
- [ ] Task 2 (AC: #1, #3): Remove "Return of Capital" from the seed script.
  - [ ] 2.1 In `scripts/seed-postgres.ts`, remove the line `{ name: 'Return of Capital' },` (line 97) from the `divDepositTypes` array.
  - [ ] 2.2 Verify the array now has 3 entries: Dividend, Interest, Capital Gains.
  - [ ] 2.3 Run `tsx scripts/seed-postgres.ts` against a clean dev database to confirm no errors.
- [ ] Task 3 (AC: #2, #3): Remove "Return of Capital" from documentation.
  - [ ] 3.1 In `docs/data-models.md` line 222, remove `"Return of Capital"` from the example comment. Result: `// e.g. "Dividend", "Capital Gain"`.
  - [ ] 3.2 Verify no other doc file references "Return of Capital" as a deposit type.
- [ ] Task 4 (AC: #3, #6): Verify no residual references and tests pass.
  - [ ] 4.1 Run `grep -ri "return of capital" --exclude-dir=node_modules --exclude-dir=_bmad-output --exclude-dir=.git .` and confirm zero matches.
  - [ ] 4.2 Run the seed spec (`scripts/seed-postgres.spec.ts`) — the tests unskipped in Task 1 must now pass. If a test fails, fix the implementation to match the test; never weaken or re-skip the test.
  - [ ] 4.3 Run `nx test server` and confirm no server tests break (canonical-seed guards stay green).

## Dev Notes

- Cites AD-7 (Dividend/Deposit only; "Return-of-capital is folded into dividend"), AD-1 (layered route-domain → Prisma direct), FR-5, FR-6, FR-7.
- The removal surface is exactly 2 lines: `scripts/seed-postgres.ts:97` and `docs/data-models.md:222`. No client code, server route, or spec hardcodes "Return of Capital" — the client renders type names dynamically from DB data.
- The `div-dep-modal` component (`apps/dms-material/src/app/account-panel/div-dep-modal/div-dep-modal.component.html` lines 71–74) renders deposit-type options from `selectDivDepositTypes` store selector. Once "Return of Capital" is removed from the DB seed, it will no longer appear in the UI. No client code change needed.
- AD-7 flags the `fidelity-data-mapper.function.ts` "Cash Deposit" label as known drift (explicitly kept, not removed). Do NOT touch that file in this story.
- The seed script also seeds "Interest" and "Capital Gains" which AD-7 flags as drift. This is a separate concern — do NOT remove those in this story. The scope is strictly "Return of Capital" only.
- The `div-deposit-types` route (`apps/server/src/app/routes/div-deposit-types/index.ts`) has only a POST route for listing types by id. No add/delete/update routes exist. No route changes needed.
- Verbatim pointer from Epic prose: "including seeding of the divDepositTypes table" — this is the `seedDivDepositTypes` method in `scripts/seed-postgres.ts`.

### Project Structure Notes

- Files to modify: `scripts/seed-postgres.ts` (remove 1 line), `docs/data-models.md` (edit 1 comment).
- No new files created. No new routes, no client changes, no prisma schema changes.
- The `divDepositType` prisma model (`prisma/schema.prisma` lines 96–104) has `name String` with no enum constraint — removal is purely a data/seed change, no migration needed.
- Alignment: this is a data-seed cleanup. The monorepo convention is that seeds live in `scripts/` and are run manually via `tsx`.

### References

- [Source: .github/epic-descriptions/epics-2026-07-14.md#remove-return-of-capital]
- [Source: architecture/architecture-dms-workspace-2026-08-22/ARCHITECTURE-SPINE.md#ad-7]
- [Source: prds/prd-dms-workspace-2026-08-20/prd.md#fr-5]
- [Source: prds/prd-dms-workspace-2026-08-20/prd.md#fr-7]
- [Source: scripts/seed-postgres.ts#seedDivDepositTypes]
- [Source: docs/data-models.md#divDepositType]
- [Source: apps/server/src/app/routes/top/get-top-div-deposit-types.function.ts]

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List
