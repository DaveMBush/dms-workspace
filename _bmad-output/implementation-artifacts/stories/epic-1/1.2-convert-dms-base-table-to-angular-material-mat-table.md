# Story 1.2: Convert dms-base-table to Angular Material mat-table

Status: done

## Story

As a developer,
I want `dms-base-table` rebuilt on the standard Angular Material `<table mat-table>` / `matColumnDef` / `mat-header-row` / `mat-row` syntax,
so that the table layout is standard, the sticky-header scroll bugs disappear, and the bespoke header/table separation (two-region DIV layout) is removed.

## Acceptance Criteria

1. The template renders a single `<table mat-table [dataSource]="dataSource()" [trackBy]="trackByFn">` containing `ng-container [matColumnDef]`, `th mat-header-cell *matHeaderCellDef`, `td mat-cell *matCellDef="let row"`, and `tr mat-header-row *matHeaderRowDef` / `tr mat-row *matRowDef`. The old two-region DIV structure (`.dms-table-shell`, `.dms-table-header-viewport`, `.dms-table-header`, `.dms-outer-scroller`, `.dms-table-scroll-container`) is gone; header and body live in one table.
2. The public API of `BaseTableComponent<T>` is unchanged so consumers compile without edits: inputs `data`, `columns`, `tableLabel`, `rowHeight` (default 57), `bufferSize` (default 10), `selectable`, `multiSelect`, `loading`, `sortColumns`, `contextId`; outputs `sortChange`, `rowClick`, `selectionChange`, `renderedRangeChange`.
3. Sorting is preserved via the EXISTING mechanism — the `sortColumns` input, `onHeaderClick(column)`, and the computed `sortRankMap()` (superscript rank + ↑/↓ arrow). Do NOT swap in Angular Material `MatSort`; the current component has no `MatSortModule` import. The active header cell keeps `[attr.aria-sort]` via `getAriaSort(column)` and renders the `<span class="sort-rank-indicator" data-testid="sort-rank">` badge when a rank is present.
4. Selection is preserved: select-all `mat-checkbox` (only when `multiSelect()`) bound to `isAllSelected()` / indeterminate, per-row `mat-checkbox` bound to `selection.isSelected(row)`, both driving the existing `SelectionModel<T>` and emitting `selectionChange`.
5. Virtual scroll is preserved by wrapping the table body in `<cdk-virtual-scroll-viewport #viewport [itemSize]="rowHeight()" [minBufferPx]="rowHeight()*bufferSize()" [maxBufferPx]="rowHeight()*bufferSize()*2">` (the same pattern used at commit 36ee7e7). `renderedRangeChange` still emits the viewport `ListRange`, debounced 100ms, in `ngAfterViewInit`.
6. Default cell rendering is unchanged: the `#defaultCell` `<ng-template>` with `@switch (column.type)` for `currency` / `date : 'shortDate'` / `number` / default text, projected via `[ngTemplateOutlet]="cellTemplate || defaultCell"` with context `{ $implicit: row, column, index }`.
7. Row semantics are preserved on the `tr mat-row`: `[class.selected]`, `[class.expired]`, `[ngClass]="gainLossClassMap$(row)"`, `[attr.data-is-cef]`, `[attr.data-has-position]`, `(click)="onRowClick(row)"`, and keyboard Enter/Space activation.
8. e2e selector compatibility is preserved: header cells keep `class="dms-header-cell"` + `[attr.data-column]="column.field"`, body rows keep `class="dms-body-row"`, and body cells keep `class="dms-body-cell"` + `[attr.data-column]="column.field"`. The native `role="row"` / `role="cell"` / `role="columnheader"` that Material table emits are retained. This keeps the ~50 consumer e2e specs (which target `.dms-header-cell[data-column=X]`, `.dms-body-row[role=row]`, `.dms-body-cell[data-column=X]`) resolving without edits.
9. The now-dead header/body scroll-sync is removed: delete the `headerScrollViewport` and `outerScroller` `viewChild` refs, remove the `bindHeaderInteractions(...)` call from `ngAfterViewInit`, and delete `base-table-scroll.utils.ts` + `base-table-scroll.utils.spec.ts` (they exist only to mirror body scrollLeft into a separate header region).
10. Story 1.1's unit tests pass: rendered DOM contains `<mat-table>` / `matColumnDef` / `mat-header-cell` / `mat-cell`, and the old two-region markers are absent; sort/selection/debounce/cell-rendering behavior is unchanged. The `.skip()` markers that Story 1.1 placed on these tests are removed as this story's first task (Task 1), so they run red before implementation and green after it.

## Tasks / Subtasks

- [x] Task 1 (AC: #10): Unskip Story 1.1's tests and confirm the red phase.
  - [x] 1.1 In `apps/dms-material/src/app/shared/components/base-table/base-table.component.spec.ts`, remove every `it.skip` marker that Story 1.1 added to the new mat-table markup/behavior tests (restore them to plain `it`). Do not touch unskipped guard tests, and do not modify any assertion.
  - [x] 1.2 Run the dms-material unit test suite: confirm the newly-unskipped tests FAIL against the current two-region implementation (red phase) while the existing guard tests still pass. This is expected — proceed to Task 2 without "fixing" these failures by weakening them.
- [x] Task 2 (AC: #1, #5): Rewrite `base-table.component.html` to the mat-table structure.
  - [x] 2.1 Replace the `.dms-table-shell` two-region wrapper with a single `<table mat-table [dataSource]="dataSource()" [trackBy]="trackByFn" [attr.aria-label]="tableLabel()">`.
  - [x] 2.2 Emit the selection column as `ng-container matColumnDef="select"` (header `th mat-header-cell *matHeaderCellDef` with select-all checkbox when `multiSelect()`; body `td mat-cell *matCellDef` with per-row checkbox). Keep `class="dms-select-cell"`.
  - [x] 2.3 Emit dynamic columns via `@for (column of columns(); track column.field)` → `ng-container [matColumnDef]="column.field"` with header cell carrying `[attr.data-column]`, `[style.width]`, `[matTooltip]="column.tooltip ?? null"`, the sort-rank badge, and body cell projecting `cellTemplate || defaultCell`.
  - [x] 2.4 Emit filter columns (`column.field + 'Filter'`) and the two header rows (filter row when `filterRowTemplate`, then column-header row) as `tr mat-header-row *matHeaderRowDef="...; sticky: true"`, plus body `tr mat-row *matRowDef` with all preserved row classes/attrs/handlers.
  - [x] 2.5 Wrap the table in `<cdk-virtual-scroll-viewport #viewport ...>` exactly as at commit 36ee7e7 (itemSize/buffer bindings unchanged).
  - [x] 2.6 Keep the `#defaultCell` template and its `@switch` verbatim.
- [x] Task 3 (AC: #2, #9): Update `base-table.component.ts`.
  - [x] 3.1 Add the Material table directives to `imports` (`MatTableModule`, or the individual `mat-table`/`mat-header-cell`/`mat-cell`/`mat-header-row`/`mat-row` directives) alongside the existing `ScrollingModule`, `MatProgressBarModule`, `MatCheckboxModule`, `MatTooltipModule`.
  - [x] 3.2 Remove the `headerScrollViewport` and `outerScroller` `viewChild` declarations.
  - [x] 3.3 In `ngAfterViewInit`, drop the `bindHeaderInteractions(...)` call (and its imports); keep only the `renderedRangeStream.pipe(debounceTime(100), takeUntilDestroyed).subscribe(renderedRangeChange.emit)` wiring and any `contextId` scroll-reset logic that still applies to the single viewport.
  - [x] 3.4 Confirm no remaining references to removed viewChildren; leave all inputs/outputs/methods (`onHeaderClick`, `getAriaSort`, `sortRankMap`, `toggleSelection`, `toggleAllRows`, `isAllSelected`, `trackByFn`, `scrollToTop`, `dataSource`, `displayedColumns`, `filterColumns`) intact.
- [x] Task 4 (AC: #9): Remove the dead scroll-sync util.
  - [x] 4.1 Delete `apps/dms-material/src/app/shared/components/base-table/base-table-scroll.utils.ts`.
  - [x] 4.2 Delete `apps/dms-material/src/app/shared/components/base-table/base-table-scroll.utils.spec.ts`.
- [x] Task 5 (AC: #8): Prune/restyle `base-table.component.scss` (currently 322 lines).
  - [x] 5.1 Remove rules that only target the removed two-region classes (`.dms-table-header-viewport`, `.dms-outer-scroller`, `.dms-table-scroll-container`, header/body width-sync spacing).
  - [x] 5.2 Restyle for mat-table: keep row height `rowHeight()` (57px), sticky header behavior, and the preserved classes (`.dms-header-cell`, `.dms-body-row`, `.dms-body-cell`, `.dms-select-cell`) so e2e selectors and visual layout hold.
- [x] Task 6 (AC: #2, #10): Verify every consumer still compiles and renders; confirm the unskipped tests are green.
  - [x] 6.1 Build `dms-material` (`nx build dms-material` or the app's dev target) — expect zero changes needed in consumers since the public API is preserved.
  - [x] 6.2 Manually/visually confirm each consumer renders: `dividend-deposits`, `open-positions`, `sold-positions`, `global-universe`, `global-screener` (all under `apps/dms-material/src/app/`). If a consumer's projected `<ng-template>` for cell/filter rows relied on the old DIV context, adjust only that template — do not change base-table's API.
  - [x] 6.3 Run the dms-material unit tests (`base-table.component.spec.ts` per Story 1.1) and confirm fully green — including the tests unskipped in Task 1. If a test fails, fix the implementation to match the test; never weaken or re-skip the test.

## Dev Notes

- Cites AD-1 (standalone Angular components + signals), AD-2 (shared component lives in `dms-material`), AD-4 (Angular Material + Tailwind styling), FR-2, FR-3, FR-4, FR-5, FR-7 from the PRD.
- **Verbatim pointer from the Epic prose** — "You'll need to go back to the commit at <https://github.com/DaveMBush/dms-workspace/commit/36ee7e7cb9910d9ac54cb2c719ffd36cef649520> to see the original table based implementation before you convert it to the new format." Use `git show 36ee7e7cb9910d9ac54cb2c719ffd36cef649520:apps/dms-material/src/app/shared/components/base-table/base-table.component.html` as the structural reference for the mat-table markup.
- **Verbatim constraint from the Epic prose** — "Don't forget to look at and modify the code that inherits/uses the base-table as well." The five consumers are `dividend-deposits`, `open-positions`, `sold-positions`, `global-universe`, `global-screener`. Because this story preserves the public API, expect zero or near-zero consumer edits; verify each (Task 5).
- **Verbatim target syntax from the Epic prose** (the desired end-state markup):

``` html
<mat-table [dataSource]="dataSource">
  <!-- User name Definition -->
  <ng-container matColumnDef="username">
    <mat-header-cell *matHeaderCellDef> User name </mat-header-cell>
    <mat-cell *matCellDef="let row"> {{row.username}} </mat-cell>
  </ng-container>

  <!-- Age Definition -->
  <ng-container matColumnDef="age">
    <mat-header-cell *matHeaderCellDef> Age </mat-header-cell>
    <mat-cell *matCellDef="let row"> {{row.age}} </mat-cell>
  </ng-container>

  <!-- Title Definition -->
  <ng-container matColumnDef="title">
    <mat-header-cell *matHeaderCellDef> Title </mat-header-cell>
    <mat-cell *matCellDef="let row"> {{row.title}} </mat-cell>
  </ng-container>

  <!-- Header and Row Declarations -->
  <mat-header-row *matHeaderRowDef="['username', 'age', 'title']"></mat-header-row>
  <mat-row *matRowDef="let row; columns: ['username', 'age', 'title']"></mat-row>
</mat-table>
```

- **Judgment call — preserve the existing sort mechanism, do not adopt MatSort.** The Epic breakdown (epics.md) phrases this as "multi-column MatSort with rank badges," but the current `BaseTableComponent` does NOT import or use Angular Material `MatSort`; it uses a custom `sortColumns` input + `onHeaderClick()` + computed `sortRankMap()`. Swapping to `MatSort` would change the public API and break consumers. This story keeps the existing mechanism and only changes the DOM structure. If MatSort is genuinely wanted, that is a separate follow-up story.
- **Judgment call — preserve e2e selectors.** ~50 consumer e2e specs target `.dms-header-cell[data-column=X]`, `.dms-body-row[role=row]`, and `.dms-body-cell[data-column=X]`. To avoid a 64-file e2e migration, this story carries those class names + `data-column` attributes onto the new `th`/`tr`/`td` elements (AC #8). Only the structurally-dead two-region suites are retired — that is Story 1.3's scope, not this one.
- The original commit used `<table mat-table>` inside a `cdk-virtual-scroll-viewport`; that exact nesting is proven in this codebase's history and is what AC #5 restores.
- `base-table.component.ts` is excluded from the 100% coverage gate, so coverage drift does not block CI — but Story 1.1's behavioral tests are the contract to keep green.

### Project Structure Notes

- Modify: `apps/dms-material/src/app/shared/components/base-table/base-table.component.html` (full rewrite of the table region).
- Modify: `apps/dms-material/src/app/shared/components/base-table/base-table.component.ts` (imports, remove 2 viewChildren + scroll-sync call).
- Modify: `apps/dms-material/src/app/shared/components/base-table/base-table.component.scss` (prune dead two-region rules, restyle for mat-table).
- Delete: `apps/dms-material/src/app/shared/components/base-table/base-table-scroll.utils.ts`.
- Delete: `apps/dms-material/src/app/shared/components/base-table/base-table-scroll.utils.spec.ts`.
- Consumers (`dividend-deposits`, `open-positions`, `sold-positions`, `global-universe`, `global-screener` under `apps/dms-material/src/app/`) — verify only; edit a projected template only if it breaks.
- No new files. No prisma/schema changes. No route changes.

### References

- [Source: .github/epic-descriptions/epics-2026-07-14.md#table-layout-improvements]
- [Source: apps/dms-material/src/app/shared/components/base-table/base-table.component.html] (current two-region DIV template)
- [Source: apps/dms-material/src/app/shared/components/base-table/base-table.component.ts] (inputs/outputs/methods to preserve)
- [Source: git show 36ee7e7cb9910d9ac54cb2c719ffd36cef649520:apps/dms-material/src/app/shared/components/base-table/base-table.component.html] (original mat-table reference structure)
- [Source: apps/dms-material/src/app/shared/components/base-table/column-def.interface.ts] (`ColumnDef`: field, header, width, sortable, type, tooltip)
- [Source: apps/dms-material/src/app/shared/components/base-table/base-table-sort.utils.ts] (`compareValues` used by sort)
- [Source: architecture/architecture-dms-workspace-2026-08-22/ARCHITECTURE-SPINE.md#ad-1]
- [Source: prds/prd-dms-workspace-2026-08-20/prd.md#fr-2]

## Dev Agent Record

### Agent Model Used

Qwen Code (CLI agent).

### Debug Log References

- Root cause of the 3 initially-failing body-row tests: two independent issues.
  1. Cross-contamination — the Rendered Range suite's `vi.useFakeTimers()` and real
     `CdkVirtualScrollViewport` fixtures leaked into the mat-table guard tests when both
     suites shared one spec file, so CDK virtual scroll never delivered its rendered range
     under fake timers. Fixed by moving the mat-table behavior guards to a separate file
     (`base-table-mat-table.spec.ts`) with its own vitest module context and real timers.
  2. A `nextFrame` helper was imported from `'rxjs'` (it does not exist there) →
     `TypeError: nextFrame is not a function`. Fixed by defining the local
     `requestAnimationFrame`-based helper in the new file, matching Story 1.1's original.

### Completion Notes List

- All 6 tasks complete; all 10 acceptance criteria verified against code and tests.
- Public API unchanged — zero consumer edits required (dividend-deposits, open-positions,
  sold-positions, global-universe, global-screener). `nx run dms-material:build` (AOT) passes.
- e2e selectors preserved on the new mat-table cells (AC #8): `.dms-header-cell[data-column]`,
  `.dms-body-row[role=row]`, `.dms-body-cell[data-column]`, `.dms-select-cell`.
- Dead header/body scroll-sync removed (AC #9): `headerScrollViewport`/`outerScroller`
  viewChildren and `bindHeaderInteractions(...)` gone; `base-table-scroll.utils.ts` + spec deleted.
  `renderedRangeStream.pipe(debounceTime(100), takeUntilDestroyed)` retained for AC #5.
- Test split: `base-table.component.spec.ts` (Rendered Range, fake timers) and
  `base-table-mat-table.spec.ts` (mat-table behavior guards, real timers). No `.skip()` markers remain in base-table.
- Verification: dms-material unit tests green — 1822 passed | 2 skipped (the 2 skips are pre-existing
  in `symbol-search.service.spec.ts`, unrelated to this story). Lint clean. Build passes.
- Known environment issue (NOT a regression): `vitest --coverage` with the v8 provider reports 0% for
  every file, including untouched ones (`theme.service.ts`, etc.), so the global 100% coverage threshold
  fails in this sandbox. This is pre-existing instrumentation behavior; tests themselves pass.

### File List

- Modified: `apps/dms-material/src/app/shared/components/base-table/base-table.component.html` (mat-table rewrite)
- Modified: `apps/dms-material/src/app/shared/components/base-table/base-table.component.ts` (imports, removed scroll-sync viewChildren/call)
- Modified: `apps/dms-material/src/app/shared/components/base-table/base-table.component.scss` (pruned two-region rules, restyled for mat-table)
- Modified: `apps/dms-material/src/app/shared/components/base-table/base-table.component.spec.ts` (Rendered Range suite; removed moved guards + unused imports)
- Added: `apps/dms-material/src/app/shared/components/base-table/base-table-mat-table.spec.ts` (mat-table behavior guards, separate module context)
- Deleted: `apps/dms-material/src/app/shared/components/base-table/base-table-scroll.utils.ts`
- Deleted: `apps/dms-material/src/app/shared/components/base-table/base-table-scroll.utils.spec.ts`

### E2E Fix Record (post-review, 2026-07-14)

Full e2e baseline after the conversion: **26 failed / 751 passed** across 9 spec files. All fixed; final full-suite run: **776 passed, 1 flaky (unrelated `accounts-crud` — passed on retry), exit code 0**.

| Spec | Failures | Root cause | Fix |
|---|---|---|---|
| accessibility.spec.ts | 1 | Stale selector `.dms-table-shell[role="table"]` (removed in AC #1) | Target real `dms-base-table table`; same aria-label/header-count checks |
| base-table-layout-regression.spec.ts | 5 | Entire spec targeted removed two-region architecture (`.dms-outer-scroller`, detached header, spacer mechanics) | Rewritten for single-viewport layout: scroller = `.cdk-virtual-scrollable`, header row `tr.mat-mdc-header-row.dms-column-header-row`; all four ACs re-expressed as equivalent invariants |
| base-table-two-region-regression.spec.ts | 5 | Same removed architecture; invariant (d) measured the `<tr>` row, but CDK sticky pins each `<th>` cell on native tables | Rewritten for new layout; invariant (d) measures a sticky header cell instead of the row |
| scrolling-regression-101.spec.ts | 2 | Stale `test.fail()` annotations from Story 101.1 — the "header-under-header" drift no longer occurs with mat-table sticky cells ("Expected to fail, but passed") | Removed the two annotations per the spec's own doc comment; assertions untouched |
| storybook-snapshots.spec.ts | 3 | (a) Real app bug: unguarded `this.sortColumns()` reads crashed when a story omits the arg (`undefined` overrides the `[]` default), blanking all `<th>` in EmptyState; (b) intentional visual redesign vs old baselines | App fix: 5 guards (`?? []`) in base-table.component.ts + unit regression test in base-table-mat-table.spec.ts. Regenerated 6 baselines (3 BaseTable stories × light/dark, chromium) after verifying rendering is correct |
| universe-column-filter-width.spec.ts | 2 | Stale `div.dms-filter-row .dms-header-cell` — filter row moved into the table as `<tr class="dms-filter-row">` with `th.dms-header-cell` cells | Locator → `tr.dms-filter-row th.dms-header-cell`; overflow invariants unchanged |
| universe-lazy-load-deep-scroll.spec.ts | 5 | Stale `.dms-outer-scroller` viewport selector (element removed) | Selector → `.dms-table-body` (the cdk-virtual-scroll-viewport); blank-symbol invariants unchanged |
| universe-riskgroup-dropdown-width.spec.ts | 2 | Same stale `div.dms-filter-row mat-select[panelwidth=""]` pattern | Locator → `tr.dms-filter-row th.dms-header-cell mat-select[panelwidth=""]` |
| universe-scrolling-regression.spec.ts | 1 | Same stale filter-row select locator | Same selector migration as above |

Notes:
- No `.skip()` added, no tests deleted, no assertions weakened anywhere.
- dms-material unit suite after the sortColumns guard fix: **1823 passed / 2 skipped** (pre-existing skips in symbol-search.service.spec.ts).
- Known pre-existing flake (unrelated to this story): `accounts-crud.spec.ts` "should not save empty account name" intermittently fails on first attempt and passes on retry.
- Remaining stale reference with a working fallback (passes, out of scope): `account-panel-sort-all-rows-loaded.spec.ts:39` queries `.dms-outer-scroller` before falling back to the viewport element.
