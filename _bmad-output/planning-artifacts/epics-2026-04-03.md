---
stepsCompleted: [1, 2, 3, 4]
inputDocuments:
  - ".github/epic descriptions/epics-2026-04-03.md"
  - "_bmad-output/planning-artifacts/epics-2026-04-02.md"
  - "_bmad-output/planning-artifacts/architecture.md"
  - "_bmad-output/project-context.md"
  - "_bmad-output/implementation-artifacts/sprint-status.yaml"
generatedAt: '2026-04-03'
project_name: dms-workspace
user_name: Dave
lastEpicNumber: 48
---

# dms-workspace — Epic Breakdown (2026-04-03)

## Overview

Six new epics derived from the 2026-04-03 epic descriptions file. This batch extends the project
backlog (Epics 1–42) starting at **Epic 43**. The work covers: fixing secondary sort on the
Universe screen, eliminating persistent janky scrolling across all tables, removing all remaining
Yahoo Finance dividend retrieval code, fixing column filter widths on the Universe screen,
correcting lazy loading behaviour across all tables, and adding CSV import support for stock
splits.

All stories follow the existing project conventions:
- **Angular 21 zoneless** — `inject()`, `OnPush`, signal-first
- **SmartNgRX / SmartSignals** for state management and lazy loading
- **Vitest** for unit tests, **Playwright** for e2e
- **Tests are authoritative** — do not change tests, fix the code
- **`pnpm all`** must pass after every story
- **Playwright MCP server** must be used to verify UI fixes and create e2e tests where specified

---

## Requirements Inventory

### Functional Requirements

| # | Source Requirement | Epic(s) |
|---|-------------------|---------|
| R1 | Secondary sort column applied on the server when primary-sort values are tied | Epic 43 |
| R2 | Playwright e2e tests verify secondary sort behaviour on the Universe screen | Epic 43 |
| R3 | Janky / stuttering scrolling eliminated across all data tables | Epic 44 |
| R4 | Playwright e2e tests verify smooth scrolling on affected table screens | Epic 44 |
| R5 | All Yahoo Finance dividend retrieval code removed from the codebase | Epic 45 |
| R6 | All dividend data (incl. current dividend) retrieved exclusively from dividendhistory.org | Epic 45 |
| R7 | Column filter inputs on Universe screen constrained by column width (not clipped) | Epic 46 |
| R8 | Column filter behaviour on Universe screen matches Account > Open/Sold Positions screens | Epic 46 |
| R9 | Playwright e2e tests verify column filter width on Universe screen | Epic 46 |
| R10 | Data loaded incrementally as rows scroll into the viewport on all tables | Epic 47 |
| R11 | No bulk/full data load triggered after the initial first-page fetch | Epic 47 |
| R12 | Playwright e2e tests and network traffic inspection confirm true lazy loading | Epic 47 |
| R13 | CSV import detects split transactions from description field containing "SPLIT" | Epic 48 |
| R14 | Split ratio calculated from pre-split open position quantity vs CSV post-split quantity | Epic 48 |
| R15 | All open position lots for the split symbol have prices and quantities adjusted by ratio | Epic 48 |
| R16 | Fractional share remainders from splits treated as a sale | Epic 48 |
| R17 | "IN LIEU OF FRX SHARE" CSV rows are intentionally skipped (no duplicate sale) | Epic 48 |
| R18 | E2E tests verify the complete CSV split handling workflow end-to-end | Epic 48 |

### Non-Functional Requirements

| # | Requirement |
|---|------------|
| NFR1 | `pnpm all` must pass after every story |
| NFR2 | Existing passing tests must not regress |
| NFR3 | Playwright MCP server must be used to reproduce issues and verify UI fixes |
| NFR4 | Network traffic must be inspected via browser dev tools to confirm lazy loading behaviour |

### Additional Requirements (Architecture)

- Secondary sort fix targets server-side ORDER BY query logic; the client already sends correct multi-column sort params
- Scrolling fix must be applied to every data table across all screens, not just one
- Dividend code audit must cover both display paths (Universe screen, Account screens) and any calculation/data-pipeline paths
- Column filter width fix should adopt the same pattern already working on Account > Open Positions and Sold Positions screens
- Lazy loading uses SmartNgRX virtual scroll — the fix must address the underlying data-fetch trigger that causes the bulk load
- CSV split detection relies on the description field; CUSIP-to-ticker resolution is already handled by existing code
- Split ratio derived as `totalCurrentOpenQuantity / csvPostSplitQuantity`; handles both forward and reverse splits

---

## Epic List

| # | Title | Est. Stories |
|---|-------|--------------|
| 43 | Fix Secondary Sort on Universe Screen | 2 |
| 44 | Fix Janky Scrolling Across All Tables | 3 |
| 45 | Remove Yahoo Finance Dividend Code | 2 |
| 46 | Fix Column Filter Width on Universe Screen | 2 |
| 47 | Fix Lazy Loading of Data on All Tables | 3 |
| 48 | CSV Import Split Handling | 5 |

---

## Epic 43: Fix Secondary Sort on Universe Screen

**Goal:** The server must honour every sort column in the multi-column sort request and apply them
in priority order. When primary-sort values are tied, the secondary (and further) sort columns
must determine the final order. The client already sends the correct parameters; the issue is
entirely server-side.

---

### Story 43.1: Diagnose and Fix Server-Side Secondary Sort

**Slug:** `43-1-diagnose-fix-server-secondary-sort`

As a trader,
I want the Universe screen results to be correctly ordered by the secondary sort column when
primary-sort values are tied,
so that I can meaningfully compare securities with equal primary-sort values.

**Acceptance Criteria:**

**Given** the Universe screen has rows with identical "Avg Purch Yield%" values,
**When** the user clicks "Avg Purch Yield%" to set it as the primary sort and Shift+clicks
"Yield%" to add it as a secondary sort (both descending),
**Then** rows with equal "Avg Purch Yield%" are sub-ordered in descending "Yield%" order.

**Given** the server receives a multi-column sort request with primary and secondary columns,
**When** the sort query is constructed,
**Then** all requested sort columns are included in the ORDER BY clause in priority order.

**Given** only a primary sort column is selected (no secondary),
**When** the sort query is constructed,
**Then** the single-column sort behaviour is unchanged.

**Given** all changes are applied,
**When** `pnpm all` runs,
**Then** all unit tests pass with no regressions.

**Definition of Done:**
- [ ] Server-side ORDER BY clause correctly includes all sort columns in their requested priority order
- [ ] Manual verification on Universe screen confirms secondary sort is visibly applied when primary values are tied
- [ ] Existing single-column sort behaviour is unaffected
- [ ] `pnpm all` passes

---

### Story 43.2: Write Playwright E2E Tests for Secondary Sort

**Slug:** `43-2-e2e-secondary-sort-universe`

As a developer,
I want Playwright e2e tests that verify multi-column sort on the Universe screen,
so that secondary sort behaviour cannot silently regress.

**Acceptance Criteria:**

**Given** the Universe screen is open with data loaded,
**When** the Playwright test clicks a primary sort column and then Shift+clicks a secondary sort
column,
**Then** the test asserts that rows with equal primary values appear in the correct secondary-sort
order.

**Given** the Playwright MCP server is used to create and run the tests,
**When** `pnpm run e2e:dms-material:chromium` is executed,
**Then** all new secondary sort tests pass with zero failures.

**Given** the secondary sort tests are added,
**When** `pnpm all` runs,
**Then** no existing tests regress.

**Definition of Done:**
- [ ] Playwright e2e tests for secondary sort created using the Playwright MCP server
- [ ] Tests cover at least one ascending and one descending secondary sort scenario
- [ ] `pnpm run e2e:dms-material:chromium` passes with the new tests included
- [ ] `pnpm all` passes

---

## Epic 44: Fix Janky Scrolling Across All Tables

**Goal:** Eliminate the persistent janky / stuttering scrolling that affects all data tables in
the application. This issue was targeted in Epic 31 but was not fully resolved and continues
across every table screen.

---

### Story 44.1: Reproduce and Diagnose Janky Scrolling with Playwright

**Slug:** `44-1-reproduce-diagnose-janky-scrolling`

As a developer,
I want to reproduce the janky scrolling issue using the Playwright MCP server and identify its
root cause,
so that I can apply a targeted, permanent fix.

**Acceptance Criteria:**

**Given** the Playwright MCP server is connected and a data table screen is open,
**When** the developer programmatically scrolls through a data table (e.g., the Universe screen
or Dividend Deposits),
**Then** the scrolling stutter / jank is observable and reproducible in the Playwright session.

**Given** the jank is reproducible,
**When** the rendering strategy, change detection cycle, virtual scroll configuration, and CSS are
inspected,
**Then** the specific root cause is identified and documented (code comment or investigation note).

**Definition of Done:**
- [ ] Jank reproducible via Playwright MCP server on at least two different table screens
- [ ] Root cause identified and noted (e.g., in a code comment or investigation note committed alongside Story 44.2)
- [ ] `pnpm all` passes (no implementation changes yet in this story)

---

### Story 44.2: Fix Janky Scrolling Implementation

**Slug:** `44-2-fix-janky-scrolling`

As a trader,
I want all data table screens to scroll smoothly without visual stuttering,
so that I can review large lists of data comfortably.

**Acceptance Criteria:**

**Given** the root cause is known from Story 44.1,
**When** the fix is applied to all affected table components,
**Then** scrolling is visually smooth with no perceptible stutter on any data table.

**Given** the fix is applied,
**When** the Playwright MCP server is used to scroll through a table,
**Then** no rendering jank is observable during the scroll.

**Given** all changes,
**When** `pnpm all` runs,
**Then** all unit tests and existing e2e tests continue to pass with no regressions.

**Definition of Done:**
- [ ] Scrolling fix applied to all data table components across all screens
- [ ] Jank is no longer reproducible via Playwright MCP server
- [ ] `pnpm all` passes

---

### Story 44.3: Verify Scrolling Fix and Write E2E Tests with Playwright

**Slug:** `44-3-verify-scrolling-e2e-tests`

As a developer,
I want Playwright e2e tests that verify smooth scrolling on all major data table screens,
so that the janky scrolling regression cannot go undetected.

**Acceptance Criteria:**

**Given** the Playwright MCP server is used to verify the fix on all major table screens,
**When** each table is scrolled end-to-end,
**Then** no stutter or rendering jank is detected.

**Given** e2e tests are written using the Playwright MCP server,
**When** `pnpm run e2e:dms-material:chromium` is run,
**Then** all new scrolling tests pass.

**Given** the new tests are added,
**When** `pnpm all` runs,
**Then** no existing tests regress.

**Definition of Done:**
- [ ] Playwright MCP server used to verify smooth scrolling on all major table screens
- [ ] E2E tests written for smooth scrolling on at least the Universe screen, Account > Open Positions, and Dividend Deposits
- [ ] `pnpm run e2e:dms-material:chromium` passes with all new scrolling tests
- [ ] `pnpm all` passes

---

## Epic 45: Remove Yahoo Finance Dividend Code

**Goal:** All dividend information — current dividend display on Universe and Account screens,
dividend calculations, and all dividend data retrieval — must exclusively use
dividendhistory.org. Every remaining Yahoo Finance dividend code path must be removed.

---

### Story 45.1: Audit and Remove Yahoo Finance Dividend Retrieval Code

**Slug:** `45-1-remove-yahoo-finance-dividend-code`

As a developer,
I want all Yahoo Finance dividend retrieval code removed from the codebase,
so that the application relies solely on dividendhistory.org for all dividend data.

**Acceptance Criteria:**

**Given** a full-codebase search for Yahoo Finance dividend retrieval (API calls, service methods,
HTTP endpoints, constants, env vars),
**When** every occurrence is identified,
**Then** it is either deleted (if unused) or replaced with the equivalent dividendhistory.org
implementation.

**Given** the current dividend displayed on the Universe screen,
**When** the screen data is loaded after the change,
**Then** the current dividend value is sourced exclusively from dividendhistory.org.

**Given** the current dividend displayed on Account screens,
**When** the screen data is loaded after the change,
**Then** the current dividend value is sourced exclusively from dividendhistory.org.

**Given** any dividend calculation path in the codebase,
**When** dividend data is accessed,
**Then** it originates from dividendhistory.org, not Yahoo Finance.

**Given** all changes are applied,
**When** `pnpm all` runs,
**Then** all unit tests pass with no regressions.

**Definition of Done:**
- [ ] Zero references to Yahoo Finance dividend retrieval remain in the codebase (confirmed via search)
- [ ] Current dividend on Universe screen sourced from dividendhistory.org
- [ ] Current dividend on Account screens sourced from dividendhistory.org
- [ ] All dividend calculation paths use dividendhistory.org data
- [ ] `pnpm all` passes

---

### Story 45.2: Verify Dividend Data Correctness After Yahoo Finance Removal

**Slug:** `45-2-verify-dividend-data-correctness`

As a trader,
I want to confirm that all dividend information displayed in the app remains correct after the
Yahoo Finance code removal,
so that my yield calculations and income projections stay accurate.

**Acceptance Criteria:**

**Given** Yahoo Finance dividend code has been removed (Story 45.1 complete),
**When** the Universe screen loads,
**Then** current dividend values are populated and visible for securities that have data on
dividendhistory.org.

**Given** the Account screens load,
**When** dividend information is displayed,
**Then** values are present, non-null, and consistent with dividendhistory.org data.

**Given** all changes,
**When** `pnpm all` runs,
**Then** all unit and integration tests pass.

**Definition of Done:**
- [ ] Manual verification confirms dividend values appear correctly on the Universe screen
- [ ] Manual verification confirms dividend values appear correctly on Account screens
- [ ] No null / undefined dividend values where data should be present
- [ ] `pnpm all` passes

---

## Epic 46: Fix Column Filter Width on Universe Screen

**Goal:** Column filter input fields on the Universe screen must be constrained within their
column's width — matching the correct behaviour already present on the Account > Open Positions
and Sold Positions screens — rather than being visually clipped by the column boundary.

---

### Story 46.1: Investigate Account Screen Column Filter and Apply Fix to Universe Screen

**Slug:** `46-1-fix-universe-column-filter-width`

As a trader,
I want column filter inputs on the Universe screen to be visually contained within their column's
width,
so that filters are usable and consistent with the Account screens.

**Acceptance Criteria:**

**Given** the Account > Open Positions screen column filter component and its CSS,
**When** the code that constrains the filter input to the column width is identified,
**Then** the same pattern is applied to the Universe screen column filter component.

**Given** the fix is applied to the Universe screen column filters,
**When** a column filter input is displayed on a narrow column,
**Then** the input is visually contained within the column boundary and is not clipped.

**Given** narrow columns on the Universe screen with an active filter,
**When** the filter input would exceed the column width,
**Then** the input scrolls or truncates inside the column boundary rather than overflowing.

**Given** all changes,
**When** `pnpm all` runs,
**Then** all unit tests pass.

**Definition of Done:**
- [ ] Universe screen column filter inputs contained within column width (no visual clipping)
- [ ] Behaviour matches Account > Open Positions and Sold Positions screens
- [ ] `pnpm all` passes

---

### Story 46.2: Verify Column Filter Fix with Playwright E2E Tests

**Slug:** `46-2-e2e-universe-column-filter-width`

As a developer,
I want Playwright e2e tests that verify column filter width behaviour on the Universe screen,
so that filter clipping cannot regress unnoticed.

**Acceptance Criteria:**

**Given** the Playwright MCP server is used to verify the fix,
**When** a column filter is activated on a narrow Universe screen column,
**Then** the filter input is fully visible within the column bounds with no overflow or clipping.

**Given** e2e tests are written using the Playwright MCP server,
**When** `pnpm run e2e:dms-material:chromium` is run,
**Then** all new column filter width tests pass.

**Given** the new tests are added,
**When** `pnpm all` runs,
**Then** no existing tests regress.

**Definition of Done:**
- [ ] Playwright MCP server used to verify column filter width on the Universe screen
- [ ] E2E tests written for column filter width on at least two narrow columns
- [ ] `pnpm run e2e:dms-material:chromium` passes with the new tests
- [ ] `pnpm all` passes

---

## Epic 47: Fix Lazy Loading of Data on All Tables

**Goal:** Data tables must load data incrementally as rows enter the viewport. The current broken
behaviour — load first 10 rows then immediately fetch ALL remaining rows — is unacceptable for
large datasets (e.g., Dividend Deposits with years of history) and must be replaced with correct
SmartNgRX virtual-scroll lazy loading.

---

### Story 47.1: Reproduce and Diagnose the Lazy Loading Bulk-Fetch Issue

**Slug:** `47-1-reproduce-diagnose-lazy-loading`

As a developer,
I want to reproduce the bulk data fetch using Playwright MCP and network inspection,
so that I can identify the exact code path that triggers loading all remaining rows at once.

**Acceptance Criteria:**

**Given** the Playwright MCP server is connected and browser network dev tools are accessible,
**When** a data table (e.g., Dividend Deposits) is loaded and the user scrolls slightly beyond
the first 10 rows,
**Then** a network request fetching ALL remaining rows (rather than the next page) is observed in
the captured network traffic.

**Given** the bulk-fetch network request is identified,
**When** the SmartNgRX virtual scroll and data-fetch trigger code is traced,
**Then** the specific code path and/or configuration value that causes the bulk fetch is
identified and documented.

**Definition of Done:**
- [ ] Bulk-fetch reproduced via Playwright MCP with network traffic confirming the issue
- [ ] Root cause identified and documented (code comment or investigation note)
- [ ] `pnpm all` passes (no implementation changes yet in this story)

---

### Story 47.2: Fix SmartNgRX Lazy Loading Implementation

**Slug:** `47-2-fix-lazy-loading-implementation`

As a trader,
I want data tables to fetch only the rows entering the viewport as I scroll,
so that the application remains fast and memory-efficient even with years of data.

**Acceptance Criteria:**

**Given** a data table with hundreds of rows,
**When** the table first loads,
**Then** only the first viewport-sized page of data is fetched from the server.

**Given** the user scrolls down,
**When** new rows are about to enter the viewport,
**Then** only the next required page is fetched — no bulk request for all remaining rows.

**Given** the user stops scrolling mid-table and does not scroll further,
**When** network traffic is inspected,
**Then** no additional data requests are made beyond the pages already needed for the visible
viewport.

**Given** the fix is applied to all data tables across all screens,
**When** `pnpm all` runs,
**Then** all unit tests pass with no regressions.

**Definition of Done:**
- [ ] Lazy loading implemented correctly — only viewport-visible pages fetched on demand
- [ ] Fix applied to all data table components across all screens (Universe, Account, Dividend Deposits, etc.)
- [ ] Bulk-fetch is no longer reproduced by Playwright MCP
- [ ] `pnpm all` passes

---

### Story 47.3: Verify Lazy Loading with Playwright E2E Tests and Network Inspection

**Slug:** `47-3-verify-lazy-loading-e2e`

As a developer,
I want Playwright e2e tests with network traffic assertions that confirm true lazy loading,
so that the bulk-fetch regression is caught automatically if it recurs.

**Acceptance Criteria:**

**Given** the Playwright MCP server with network traffic inspection enabled,
**When** a data table is loaded and the user scrolls through it page by page,
**Then** network requests are made incrementally (one page-sized request per scroll step) and no
single request fetching all remaining rows is made.

**Given** e2e tests using the Playwright MCP server with network assertions,
**When** `pnpm run e2e:dms-material:chromium` is run,
**Then** all new lazy loading tests pass including the network-traffic assertions.

**Given** the Dividend Deposits screen (highest data volume risk),
**When** the table is loaded and scrolled,
**Then** network inspection confirms only incremental page fetches occur throughout the scroll.

**Given** the new tests are added,
**When** `pnpm all` runs,
**Then** no existing tests regress.

**Definition of Done:**
- [ ] Playwright MCP server used to verify lazy loading with network traffic on at least the Universe, Account Open Positions, and Dividend Deposits screens
- [ ] E2E tests written that include network-traffic assertions for incremental paginated requests
- [ ] `pnpm run e2e:dms-material:chromium` passes with all new lazy-loading tests
- [ ] `pnpm all` passes

---

## Epic 48: CSV Import Split Handling

**Goal:** The CSV import correctly identifies stock split transactions, calculates the split ratio
from current open position data, adjusts all open lot quantities and prices for the affected
symbol, records fractional-share remainders as sales, and intentionally skips "IN LIEU OF FRX
SHARE" rows to avoid duplicate entries.

---

### Story 48.1: Detect and Classify Split Transactions in CSV Import

**Slug:** `48-1-detect-split-transactions-csv`

As a trader,
I want the CSV import to detect split transaction rows automatically,
so that they are processed with the correct split-handling logic rather than as ordinary buy or
sell trades.

**Acceptance Criteria:**

**Given** a CSV row whose description contains the word "SPLIT" (e.g.,
"REVERSE SPLIT R/S FROM 691543102#REOR M005168075001"),
**When** the CSV import parser processes that row,
**Then** the row is classified as a split transaction and is not processed as a buy or sell.

**Given** a CSV row classified as a split,
**When** the row is passed into the processing pipeline,
**Then** the symbol (ticker) and the new post-split quantity are extracted correctly from the row.

**Given** a CSV row whose description does not contain "SPLIT",
**When** the row is processed,
**Then** it is not classified as a split and is handled by the existing buy/sell logic unchanged.

**Given** all changes,
**When** `pnpm all` runs,
**Then** all unit tests (including new unit tests for split detection) pass.

**Definition of Done:**
- [ ] CSV parser correctly classifies rows with "SPLIT" in the description as split transactions
- [ ] Symbol and post-split quantity extracted correctly from split rows
- [ ] Existing buy/sell logic unaffected for non-split rows
- [ ] Unit tests written for split detection (including edge cases: no "SPLIT", mixed case, etc.)
- [ ] `pnpm all` passes

---

### Story 48.2: Calculate Split Ratio from Open Position and CSV Data

**Slug:** `48-2-calculate-split-ratio`

As a trader,
I want the split ratio to be automatically derived from existing position data and the CSV
post-split quantity,
so that the correct adjustment factor is applied without manual input.

**Acceptance Criteria:**

**Given** a reverse split for OXLC where the CSV post-split quantity is 306 and the total current
open position quantity across all lots is 1530,
**When** the ratio is calculated as `1530 / 306`,
**Then** the system determines a ratio of 5 (i.e., a 1-for-5 reverse split).

**Given** a forward split where the current open quantity is 100 and the CSV post-split quantity
is 200,
**When** the ratio is calculated as `100 / 200 = 0.5`,
**Then** the system correctly identifies it as a 2-for-1 forward split.

**Given** no open lots exist for the split symbol at the time of processing,
**When** the ratio calculation is attempted,
**Then** the split row is skipped with a warning logged and no exception is thrown.

**Given** all changes,
**When** `pnpm all` runs,
**Then** all unit tests (including new unit tests for ratio calculation) pass.

**Definition of Done:**
- [ ] Split ratio calculated as `totalCurrentOpenQuantity / csvPostSplitQuantity`
- [ ] Correctly handles both forward splits (ratio < 1) and reverse splits (ratio > 1)
- [ ] Gracefully skips with a warning when no open position exists for the symbol
- [ ] Unit tests cover ratio calculation, forward split, reverse split, and missing-position edge cases
- [ ] `pnpm all` passes

---

### Story 48.3: Adjust Existing Open Position Lots per Split Ratio

**Slug:** `48-3-adjust-positions-for-split`

As a trader,
I want all open position lots for a split symbol to have their quantities and per-share prices
adjusted by the split ratio,
so that my cost basis and share count accurately reflect the post-split reality.

**Acceptance Criteria:**

**Given** a 1-for-5 reverse split for OXLC (ratio = 5) with multiple open lots,
**When** the split adjustment is applied to all open OXLC lots,
**Then** each lot's quantity is divided by 5 and each lot's per-share price is multiplied by 5.

**Given** a 2-for-1 forward split (ratio = 0.5) for any symbol,
**When** the split adjustment is applied,
**Then** each lot's quantity is doubled and each lot's per-share price is halved.

**Given** a split adjustment is applied to a lot,
**When** the total position value is recalculated (quantity × per-share price),
**Then** the total value is unchanged from before the split (the adjustment is value-neutral).

**Given** all changes,
**When** `pnpm all` runs,
**Then** all unit tests pass.

**Definition of Done:**
- [ ] All open lots for the split symbol have quantity divided by ratio and per-share price multiplied by ratio
- [ ] Total position value is invariant under the adjustment (no wealth created or destroyed)
- [ ] Database update applied correctly to all affected lot records
- [ ] Unit tests cover both forward and reverse split lot adjustments, including multi-lot scenarios
- [ ] `pnpm all` passes

---

### Story 48.4: Handle Fractional Share Remainders from Splits

**Slug:** `48-4-handle-split-remainders`

As a trader,
I want fractional share remainders from splits to be recorded as a sale, and the corresponding
"IN LIEU OF FRX SHARE" CSV row to be intentionally skipped,
so that my cash balance is correct and no duplicate sale entry is created.

**Acceptance Criteria:**

**Given** a reverse split that produces a fractional remainder (e.g., 1531 shares ÷ 5 = 306.2
shares, remainder = 0.2),
**When** the split adjustment is calculated,
**Then** the 0.2-share remainder is recorded as a fractional share sale at the current market
price.

**Given** a CSV row with a description containing "IN LIEU OF FRX SHARE",
**When** the CSV import processes that row,
**Then** the row is intentionally skipped (the remainder was already handled as a sale in the
split adjustment step) and no duplicate sale record is created.

**Given** a split that produces an exact whole-share result (no remainder),
**When** the split adjustment completes,
**Then** no fractional sale record is created.

**Given** all changes,
**When** `pnpm all` runs,
**Then** all unit tests pass.

**Definition of Done:**
- [ ] Fractional share remainder calculated from the adjusted quantity and recorded as a sale
- [ ] "IN LIEU OF FRX SHARE" CSV rows are intentionally skipped — no duplicate sale
- [ ] No fractional sale record created when the split produces an exact whole-share result
- [ ] Unit tests cover all three scenarios: remainder present, "IN LIEU" row skipped, no remainder
- [ ] `pnpm all` passes

---

### Story 48.5: E2E Tests for CSV Import Split Handling

**Slug:** `48-5-e2e-csv-import-split-handling`

As a developer,
I want end-to-end tests for the complete CSV import split handling workflow,
so that split processing correctness is continuously verified and cannot regress silently.

**Acceptance Criteria:**

**Given** a test CSV file containing an OXLC 1-for-5 reverse split row and a precondition of
1530 open OXLC shares across multiple lots,
**When** the CSV import is executed (via the UI or API),
**Then** the resulting OXLC open position shows 306 shares with per-share prices adjusted by
factor 5.

**Given** the test CSV also contains an "IN LIEU OF FRX SHARE" row for OXLC,
**When** the import completes,
**Then** only one fractional sale record exists for the remainder — the "IN LIEU" row did not
create a duplicate.

**Given** the import is complete,
**When** the Account > Open Positions screen is loaded,
**Then** the OXLC position reflects the correct post-split quantity (306) and adjusted cost
basis.

**Given** all e2e tests,
**When** `pnpm run e2e:dms-material:chromium` is run,
**Then** all CSV split import tests pass with zero failures.

**Given** the new tests are added,
**When** `pnpm all` runs,
**Then** no existing tests regress.

**Definition of Done:**
- [ ] E2E test fixture created with sample OXLC reverse split CSV data
- [ ] Test verifies post-split quantity (306) and adjusted per-share price basis
- [ ] Test verifies "IN LIEU OF FRX SHARE" row is skipped (no duplicate sale)
- [ ] Test verifies Account > Open Positions displays the correct post-split position
- [ ] `pnpm run e2e:dms-material:chromium` passes with all new CSV split tests
- [ ] `pnpm all` passes

---

## Sprint Status Additions

The following entries should be appended to `sprint-status.yaml`:

```yaml
  # ── Epic 43: Fix Secondary Sort on Universe Screen ───────────────────────────
  epic-43: backlog
  43-1-diagnose-fix-server-secondary-sort: backlog
  43-2-e2e-secondary-sort-universe: backlog
  epic-43-retrospective: optional

  # ── Epic 44: Fix Janky Scrolling Across All Tables ────────────────────────────
  epic-44: backlog
  44-1-reproduce-diagnose-janky-scrolling: backlog
  44-2-fix-janky-scrolling: backlog
  44-3-verify-scrolling-e2e-tests: backlog
  epic-44-retrospective: optional

  # ── Epic 45: Remove Yahoo Finance Dividend Code ───────────────────────────────
  epic-45: backlog
  45-1-remove-yahoo-finance-dividend-code: backlog
  45-2-verify-dividend-data-correctness: backlog
  epic-45-retrospective: optional

  # ── Epic 46: Fix Column Filter Width on Universe Screen ───────────────────────
  epic-46: backlog
  46-1-fix-universe-column-filter-width: backlog
  46-2-e2e-universe-column-filter-width: backlog
  epic-46-retrospective: optional

  # ── Epic 47: Fix Lazy Loading of Data on All Tables ───────────────────────────
  epic-47: backlog
  47-1-reproduce-diagnose-lazy-loading: backlog
  47-2-fix-lazy-loading-implementation: backlog
  47-3-verify-lazy-loading-e2e: backlog
  epic-47-retrospective: optional

  # ── Epic 48: CSV Import Split Handling ────────────────────────────────────────
  epic-48: backlog
  48-1-detect-split-transactions-csv: backlog
  48-2-calculate-split-ratio: backlog
  48-3-adjust-positions-for-split: backlog
  48-4-handle-split-remainders: backlog
  48-5-e2e-csv-import-split-handling: backlog
  epic-48-retrospective: optional
```
