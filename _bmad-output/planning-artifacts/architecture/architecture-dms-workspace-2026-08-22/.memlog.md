---
topic: DMS architecture spine
goal: consistency contract for brownfield DMS dividend tracker
updated: 2026-08-22T00:00
---

## Run log

### 2026-08-22 — run start

- Mode: **Fast path** (draft from codebase + PRD, `[ASSUMPTION]`/`[ADOPTED]` tags). Audience: **Just me + agent** (lean working contract for BMad dev-story execution).
- **Prior-session assumption corrections applied** (these were wrong in earlier notes, fixed this run):
  - Skill names are **lowercase** (not `Bmad-...` capitalized in invocations).
  - There is **no `libs/` directory** — shared code lives inside the apps.
  - The primary client store is **`@smarttools/smart-signals` (SmartNgRX)**, not NgRx. NgRx is used *only* for `current-account` (a scoped selection, not a second source of truth).
  - The client is built with **AnalogJS + Vite** (`@analogjs/vite-plugin-angular`), **not** Angular CLI.
  - The client app is **`apps/dms-material`**, not `apps/web`.
  - Auth is **Cognito / Amplify** (Cognito JWT + RS256/jwks-rsa), not a generic OIDC.
  - Server entry is `src/app/app.ts` (Fastify), not `main.ts`.
- Standing user directive honored: **write the spine in small chunks per `edit`** (local model, avoid write timeouts).

### 2026-08-22 — key decisions (as they landed)

- **Paradigm:** layered domain-driven — route-domain → service → Prisma (server); entity-store `Top` tree (client). Recorded in frontmatter + Design Paradigm section.
- **11 ADs drafted (AD-1..AD-11).** Each carries Binds/Prevents/Rule. AD-3, AD-4, AD-5, AD-6, AD-7, AD-8, AD-11 are `[ADOPTED]` (brownfield conventions ratified, not invented). AD-9 (capital gains computed from trade closure), AD-10 (Electron thin wrapper), AD-1, AD-2 are structural invariants.
- **Capital gains are COMPUTED** from position closure (trades buy→sell), never stored as a deposit type. No short/long-term split (FR-3 has none).
- **Deposit types = Dividend / Deposit ONLY.** `Cash Deposit` label in `fidelity-data-mapper.function.ts` is a flagged alias, not a new type (AD-7). RoC folded into dividend; no "Interest".
- **Screener = one row per symbol** (unique), bulk sync with real signals (price, yield, yield consistency); re-import matching a soft-deleted row undeletes it (AD-8).
- **`prisma/schema.prisma` is source of truth** over `data-models.md` (AD-3).
- **No repository/service seam** — handlers call Prisma directly (AD-3, `[ADOPTED]`).
- **Auth skipped when NODE_ENV ∈ development|test|local** — flagged as drift, not changed (AD-5).
- **`trades` + `divDepositType` lack `version` field** — flagged as known gap, not backfilled (AD-6).
- **`express ^5.2.1` is a legacy dep** — server runs Fastify (Stack table + Deferred).

### 2026-08-22 — draft complete

- All 8 spine sections written: frontmatter, Design Paradigm, Invariants & Rules (AD-1..AD-11), Consistency Conventions, Stack, Structural Seed, Capability→Architecture Map (FR-1..FR-19), Deferred.
- Structural Seed corrected against actual `ls` output (real server subdirs `config/ types/ utils/ volatility/`, all 18 route domains, real client dirs `shell/ account-panel/ accounts/ dashboard/ universe-settings/ auth/ error-handler/ global/ demo/`).
- Status still `draft` — proceeding to finalize protocol (2 reviewer subagents + lint + editorial review).

### 2026-08-22 — finalized

- **Adversarial two-units-incompatibility test (reviewer 1):** scanned all AD pairs + Capability Map for contradictions a dev-story would trip on. **Found + fixed the AD-3 vs `services/` ambiguity** — AD-3 said "no repository/service seam" while a top-level `services/` dir exists and the paradigm read "→ service →". Resolved: AD-3's Rule now states the top-level `services/` holds *operational* concerns only (audit-log, database-performance, cusip-cache-cleanup, auth-database-monitor; they use Prisma for their own concerns, not as a domain data-access seam), and the paradigm line was corrected to "route-domain → Prisma direct; operational services alongside". Route-domain `services/` (e.g. `import/fidelity-import-service.function.ts`) is now explicitly distinguished from the top-level operational `services/`. Verified consistent: AD-2 vs NgRx (current-account = scoped selection, not a 2nd source of truth), AD-1 (no cross-domain imports) vs AD-9 (gains computed in trades domain), AD-8 (screener un-delete) vs AD-6 (soft-delete `deletedAt`), AD-7 (`Cash Deposit` = flagged alias, not a 3rd type).
- **Tech-claims verification (reviewer 2):** grounded every Stack-table version against the authoritative source — `package.json` (root, holds all deps). **All 21 key packages match exactly:** fastify `~5.8.5`, prisma `^7.9.1`, `@prisma/adapter-better-sqlite3 ^7.2.0`, `better-sqlite3 ^12.6.2`, Angular `22.0.8`, `@angular/material 22.0.6`, `@ngrx/signals 21.0.1`, `@smarttools/smart-core` + `smart-signals 4.0.0`, `@aws-amplify/auth ^6.19.1`, `@nx/devkit 23.1.0`, `@analogjs/vite-plugin-angular 2.6.4`, `vite 8.1.3`, `typescript 6.0.3`, `vitest 4.1.0`, `@playwright/test 1.55.1`, `tailwindcss ^4.2.2`, `primeng ^21.1.1`, `electron ^43.0.0`, `express ^5.2.1` (legacy), `@smarttools/eslint-plugin 1.0.5`. Node `^24.0.0` + pnpm `11.17.0` via `engines`. No web research needed — local brownfield monorepo, the repo is the source of truth.
- **FR coverage + OQ triage:** Capability→Architecture Map covers all 19 FRs (FR-1..FR-19), each with route domain + client area + store + governing ADs. All 3 PRD OQs already resolved in the finalized PRD — none carry into the spine as open items.
- **Lint (markdown):** frontmatter valid (12 keys). **Found + fixed a real rendering bug** — the Conventions table Auth row had unescaped pipes in `development|test|local`, breaking the 2-column row; escaped to `development\|test\|local` to match the Deferred-table usage of the same phrase. (The lint's flag on the Deferred row is a false positive — naive pipe-counting vs the correct `\|` escape.) Code fences balanced (4). All other tables consistent.
- **Editorial review:** structure — section order, heading hierarchy, table integrity, no orphaned content, all clean. Prose — direct contract voice, consistent technical terms, no filler; "lean working contract" register for BMad dev-story execution held throughout.
- **`status: draft → final`**, `updated: 2026-08-22`. Spine is now a buildable consistency contract.
- **Ready for next BMad step:** `bmad-ux` (UX/design spec) → `bmad-create-epics-and-stories` (break the 19 FRs into epics + stories, each story citing the governing ADs from the Capability→Architecture Map).


