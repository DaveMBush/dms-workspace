---
topic: DMS — Dividend Management System (CEF dividend-investing tracker)
updated: 2026-08-21T20:19
---

- (decision) Stakes = personal/internal (solo CEF portfolio tool); target PRD length ~4-6 pages, lighter UJs, reviewer gate runs quietly. Source: README framing + user confirmation.
- (decision) Working mode = Fast path + Vision+Features entry (capability-first, internal tool, single operator); downscaled UJs, no standalone personas. Source: user chose 'Let me suggest'.
- (decision) Fidelity CSV import = CORE first-class capability (user confirmed working UI exists; component-inventory just missed it). Described as import of dividends/positions from Fidelity export into divDeposits/trades.
- (decision) PRD scope = CURRENT WORKING CAPABILITY ONLY (source of truth for a greenfield rebuild). In-flight tasks (base-table -> mat-table refactor epic 2026-07-14) go to addendum/Open Questions, NOT as PRD features.
- (decision by user) Return of capital is NOT tracked separately from dividends — Dave treats RoC as the same as a dividend for this app. PRD updated to collapse the 3-way income split (dividend/capital-gain/return-of-capital) into 2 (dividend/capital-gain) across Purpose, Vision, JTBD, UJ-2, Glossary, FR-5, Non-Goals, Success Metrics. Capital gain stays its own type.
- (decision by user) Screener is the PRIMARY way CEFs enter the universe (run screener, sync candidate in via GUI). Manual symbol entry (FR-9) is a SECONDARY/fallback mechanism only for symbols outside the screener's main universe. UJ-1 rewritten so it leads with screener sync + manual-add fallback; JTBD §2.1 reordered to lead with screening. Dave has already noted the related seed code change (deposit types) in his next set of epics — do not act on it.
- (decision by user) Screener sync is BULK (all qualifying CEFs in one step), not single-symbol. UJ-3, UJ-1, FR-10, Glossary "Screener" row all rewritten from singular "candidate" to "the qualifying set." Dave's real evaluation signals = current market price, yield (computed from distribution × distributions/year ÷ price), and yield consistency (last-3-not-declining). He does NOT use premium/discount, YTD return, category, or volatility — those were removed from the Glossary row, FR-10, and FR-12 (which now lists the actual cached fields: distribution, distributions/year, last price, risk group).
- (decision by user) Sold positions are NOT classified as short-term vs. long-term gain/loss — no code does that and Dave doesn't care about gain type. FR-3 consequence and Glossary "Sold position" row now say only "the gain/loss amount is shown."
- (decision by user) (decision by user) Capital gains are NOT a deposit type — they are computed from closed positions (trades buy→sell) in aggregate-account-data.function.ts. Deposits capture external cash only; seeded types are 'Dividend' and 'Deposit' (Fidelity import also creates 'Cash Deposit'). Fixed 10 PRD locations: §0 Purpose, §1 Vision, JTBD ×2, UJ-2, Glossary dividend-deposit/deposit-type rows, FR-5, FR-15, Success Metrics Income-accuracy.
- (decision by user) (decision by user) OQ-1 resolved: Fidelity import mapping derived from fidelity-data-mapper.function.ts and folded into FR-6 (DIVIDEND RECEIVED->divDeposits Dividend; ELECTRONIC FUNDS TRANSFER/MONEY LINE RECEIVED->divDeposits Cash Deposit; BUY/SELL->trades; splits handled; money-market trades skipped; unknowns collected; dates MM/DD/YYYY->ISO; accounts/symbols findOrCreate). OQ-2 resolved: prisma/schema.prisma is source of truth; data-models.md stale where disagreeing. OQ-3 resolved: one screener row per symbol; re-import matching a soft-deleted row undeletes it instead of duplicating.
- (override by user) (override by user) Deposit type set is Dividend / Deposit ONLY — not a third 'Cash Deposit' type. Fidelity EFT/MONEY LINE RECEIVED rows map to the Deposit type. (Code fidelity-data-mapper.function.ts currently creates a literal 'Cash Deposit' type — treat as drift or Fidelity-side naming; PRD reflects Dave's intent of two types.)
- (event by user) PRD finalized: status set to final. All 3 open questions resolved (OQ-1 mapping in FR-6, OQ-2 schema.prisma source of truth, OQ-3 one screener row per symbol). Deposit type set confirmed as Dividend/Deposit only. Reviewer pass complete — doc internally consistent.
