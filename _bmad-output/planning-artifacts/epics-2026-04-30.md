---
stepsCompleted: [1, 2, 3, 4]
inputDocuments:
  - '.github/epic descriptions/epics-2026-04-30.md'
  - '_bmad-output/planning-artifacts/epics-2026-04-29.md'
  - '_bmad-output/planning-artifacts/architecture.md'
  - 'apps/server/src/app/routes/universe/add-symbol/add-symbol.function.ts'
  - 'apps/server/src/app/routes/import/create-universe-entry.helper.ts'
  - 'apps/server/src/app/routes/settings/update/index.ts'
  - 'apps/server/src/app/routes/div-deposits/index.ts'
  - 'apps/server/src/app/routes/trades/index.ts'
  - 'apps/server/src/app/volatility/recalculate-universe-volatility.function.ts'
  - 'apps/server/src/app/routes/settings/common/get-distributions.function.ts'
  - 'apps/dms-material/src/app/shared/build-universe-map.function.ts'
  - 'apps/dms-material/src/app/account-panel/dividend-deposits/dividend-deposits-component.service.ts'
  - 'apps/dms-material/src/app/account-panel/open-positions/open-positions-component.service.ts'
  - 'apps/dms-material/src/app/account-panel/sold-positions/sold-positions-component.service.ts'
generatedAt: '2026-04-30'
project_name: dms-workspace
user_name: Dave
lastEpicNumber: 96
---

# dms-workspace — Epic Breakdown (2026-04-30)

## Overview

Five new epics derived from the 2026-04-30 epic descriptions file.

- **Epic 92 — Grab Volatility During Import:** The CUSIP-resolution path in the CSV import
  pipeline (`create-universe-entry.helper.ts`) calls `fetchAndUpdatePriceData` but never calls
  `recalculateUniverseVolatility`. As a result, any symbol added via the Fidelity CSV import
  has `volatility_long`, `volatility_short`, and `volatility_calculated_at` permanently unset
  until the next manual per-symbol edit or batch field update (if Epic 93 is completed). The
  fix mirrors the pattern already used in `add-symbol.function.ts`: call `getDistributions`,
  pass the full history to `recalculateUniverseVolatility`, and supply the prefetched outcome
  to `fetchAndUpdatePriceData`.

- **Epic 93 — Grab Volatility During Update Fields:** The batch field-update handler
  (`settings/update/index.ts → processUniverse`) calls `getDistributions` to refresh price
  and ex-date data but completely discards the `history` array and never calls
  `recalculateUniverseVolatility`. Clicking "Update Fields" therefore silently leaves all
  volatility figures stale. The fix is to extract `history` from the `getDistributions`
  outcome and call `recalculateUniverseVolatility(universe.id, history)` for each symbol
  processed in the batch.

- **Epic 94 — Return Symbol with divDeposit Data:** The `/api/div-deposits` POST endpoint
  returns `universeId` in every `DivDeposit` row. The client builds a `buildUniverseMap()`
  computed signal that iterates the entire universe store and constructs a
  `Map<universeId, Universe>`, which the dividend-deposits component service then uses to
  resolve each deposit's ticker symbol. Returning the resolved `symbol` string directly from
  the server eliminates the client-side map lookup, simplifies the state model, and removes
  the dependency on the universe store being fully loaded before deposits can display.

- **Epic 95 — Return Symbol with Open Positions Data:** The `Trade` interface on both client
  and server already has a `symbol?: string` field, and the SmartNgRX POST endpoint at
  `/api/trades/` returns `symbol` by joining with the `universe` table. However,
  `open-positions-component.service.ts` still calls `buildUniverseMap()` and performs a
  `universeMap.get(trade.universeId)` lookup rather than reading `trade.symbol` directly.
  This epic enforces the symbol field as always-present and removes the universe-map
  dependency from the open-positions component service.

- **Epic 96 — Return Symbol with Sold Positions Data:** The same situation as Epic 95 applies
  to the sold-positions component service: `sold-positions-component.service.ts` also uses
  `buildUniverseMap().get(trade.universeId)` instead of `trade.symbol`. After this epic and
  Epics 94–95, `buildUniverseMap` has no remaining callers and is deleted entirely.

All stories follow existing project conventions:

- **Angular 21 zoneless** — `inject()`, `OnPush`, signal-first
- **SmartNgRX / SmartSignals** for state management
- **Fastify** backend, **Prisma** ORM (SQLite dev / PostgreSQL prod)
- **Vitest** for unit tests, **Playwright** for E2E
- **Tests are authoritative** — do not change tests unless a test is asserting something we
  are explicitly changing
- **`pnpm all`** must pass after every story
- **Playwright MCP server** must be used to reproduce and verify all UI failures
- **TDD** — failing test first, then implementation, then regression-prevention coverage

---

## Requirements Inventory

### Functional Requirements

| #   | Source Requirement                                                                                                                                                                                                                      | Epic(s) |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| R48 | When a symbol is added to the universe via CSV import (CUSIP resolution path), volatility must be calculated and stored before the import response is returned                                                                          | Epic 92 |
| R49 | The volatility calculation during import must use the full dividend history returned by `getDistributions`, not a subset or empty array                                                                                                  | Epic 92 |
| R50 | Existing unit and E2E tests for the import pipeline must continue to pass; a new regression test must assert that `volatility_long` and `volatility_short` are non-null after a successful symbol import                                | Epic 92 |
| R51 | When the batch "Update Fields" operation runs (`GET /api/settings/update`), `recalculateUniverseVolatility` must be called for every active universe symbol using the full dividend history fetched during that same pass              | Epic 93 |
| R52 | The volatility recalculation in the batch update must not require a separate dividend history fetch; it must reuse the `history` array already returned by the `getDistributions` call made for distribution data                       | Epic 93 |
| R53 | `pnpm all` must pass after each Epic 93 story, including the existing settings/update unit and E2E tests                                                                                                                                | Epic 93 |
| R54 | The `GET /api/div-deposits` POST endpoint must return a `symbol` string alongside (or instead of) `universeId` for every `DivDeposit` record                                                                                           | Epic 94 |
| R55 | The client `DivDeposit` interface and all dependent components must be updated to consume `symbol` directly from the API response; the `buildUniverseMap()` call must be removed from `dividend-deposits-component.service.ts`          | Epic 94 |
| R56 | The `symbol` field in the server and client `Trade` interface must be non-optional; the SmartNgRX POST `/api/trades/` endpoint must always return a `symbol` value for every trade                                                     | Epic 95 |
| R57 | `open-positions-component.service.ts` must consume `trade.symbol` directly without calling `buildUniverseMap()` or performing any lookup against the universe store                                                                    | Epic 95 |
| R58 | `sold-positions-component.service.ts` must consume `trade.symbol` directly without calling `buildUniverseMap()` or performing any lookup against the universe store                                                                    | Epic 96 |
| R59 | After Epics 94–96 are complete, the `buildUniverseMap` computed function must be deleted from the codebase; no remaining callers may reference it                                                                                       | Epic 96 |

### Non-Functional Requirements

| #     | Requirement                                                                             |
| ----- | --------------------------------------------------------------------------------------- |
| NFR25 | `pnpm all` must pass after every story                                                  |
| NFR26 | Existing passing tests must not regress                                                 |
| NFR27 | Test assertions must not be weakened to make tests pass; root causes must be fixed      |
| NFR28 | The Playwright MCP server must be used to verify any UI changes end-to-end              |

### Additional Requirements (Architecture / Context)

- **Epic 92 — Import volatility fix:** The two code paths that create universe records during
  import are `add-symbol.function.ts` (manual add — already calls
  `recalculateUniverseVolatility` via `fetchDistributionsAndRecalculate`) and
  `create-universe-entry.helper.ts` (CUSIP CSV import — does NOT call volatility). The fix
  must restructure `createUniverseEntry` to: (1) call `getDistributions(symbol)` first,
  (2) call `recalculateUniverseVolatility(entry.id, history)` with the full history, and
  (3) call `fetchAndUpdatePriceData(entry.id, symbol, entry, { prefetchedDistributionOutcome:
  outcome })` so the distribution API is only hit once per symbol — matching the pattern in
  `add-symbol.function.ts`.

- **Epic 93 — Update-fields volatility fix:** In `settings/update/index.ts`, the
  `processUniverse` helper calls `checkForNewDistribution(universe)` which internally calls
  `getDistributions(universe.symbol)` but only destructures `{ result: newDistribution }`,
  discarding `history`. The fix requires returning `history` from
  `checkForNewDistribution` (or inlining its call), then calling
  `recalculateUniverseVolatility(universe.id, history)` as part of `processUniverse`. The
  rate-limiting delay in `dividend-history.service.ts` (10 s between requests) means the
  batch update already serialises calls; no additional throttling is needed for the
  volatility recalculation step.

- **Epic 94 — divDeposit server join:** The current Prisma query in `handleGetDivDepositsRoute`
  uses `prisma.divDeposits.findMany({ where: { id: { in: ids } } })` with no `include`. Add
  `include: { universe: { select: { symbol: true } } }` and map `u.universe?.symbol ?? null`
  to a new `symbol` field in the response. Update the `DivDeposit` interface on both the
  server (`div-deposits.interface.ts`) and the client (`div-deposit.interface.ts`) to include
  `symbol: string | null`. Do the same for the `handleAddDivDepositRoute` and
  `handleUpdateDivDepositRoute` responses.

- **Epic 95 — Trades symbol enforcement:** The SmartNgRX GET trades endpoint at
  `routes/trades/index.ts` already includes `universe: { select: { symbol: true } }` in its
  Prisma query and maps `trade.universe?.symbol` to `symbol`. Change `symbol?: string` to
  `symbol: string` in both the server `Trade` interface and the client
  `apps/dms-material/src/app/store/trades/trade.interface.ts`. In
  `open-positions-component.service.ts`, replace the `universeMap.get(trade.universeId)` call
  with `trade.symbol` and remove the `private universeMap()` helper and its `buildUniverseMap`
  call.

- **Epic 96 — Sold positions and final cleanup:** Apply the same client-side change to
  `sold-positions-component.service.ts`. Once Epics 94, 95, and 96 are all merged, delete
  `apps/dms-material/src/app/shared/build-universe-map.function.ts` entirely. Verify no
  remaining imports reference it.

### FR Coverage Map

| FR  | Covered by Story(ies) | Notes                                                                                  |
| --- | --------------------- | -------------------------------------------------------------------------------------- |
| R48 | 92.1                  | Fix `createUniverseEntry` in the CSV import path                                       |
| R49 | 92.1                  | Pass full `history` from `getDistributions` to `recalculateUniverseVolatility`         |
| R50 | 92.1, 92.2            | Unit tests in 92.1; Playwright E2E regression in 92.2                                 |
| R51 | 93.1                  | Call `recalculateUniverseVolatility` inside `processUniverse`                         |
| R52 | 93.1                  | Reuse `history` from `getDistributions`, no extra fetch                                |
| R53 | 93.1                  | `pnpm all` gate on every story                                                         |
| R54 | 94.1                  | Server-side Prisma join + interface update                                             |
| R55 | 94.2                  | Client-side component and interface update                                             |
| R56 | 95.1                  | Make `symbol` required in `Trade` interfaces on both sides                            |
| R57 | 95.2                  | Remove `buildUniverseMap` usage from open-positions component service                 |
| R58 | 96.1                  | Remove `buildUniverseMap` usage from sold-positions component service                 |
| R59 | 96.2                  | Delete `build-universe-map.function.ts` after all callers removed                     |

---

## Epic List

| #   | Title                                               | Est. Stories |
| --- | --------------------------------------------------- | ------------ |
| 92  | Grab Volatility During Import                       | 2            |
| 93  | Grab Volatility During Update Fields                | 1            |
| 94  | Return Symbol with divDeposit Data                  | 2            |
| 95  | Return Symbol with Open Positions Data              | 2            |
| 96  | Return Symbol with Sold Positions Data              | 2            |

---

## Epic 92: Grab Volatility During Import

**Goal:** Ensure volatility is calculated and persisted for every symbol added through the
CSV import pipeline (CUSIP resolution path), so all universe records have valid
`volatility_long` and `volatility_short` fields immediately after import.

---

### Story 92.1: Fix CUSIP Resolution Import Path to Capture Volatility

**Slug:** `92-1-fix-import-volatility-capture`

As a developer,
I want `createUniverseEntry` in `create-universe-entry.helper.ts` to call
`recalculateUniverseVolatility` with the full dividend history after creating a universe
record via CSV import,
So that symbols added through the Fidelity import pipeline have volatility data from day one
without requiring a subsequent batch field update.

**Acceptance Criteria:**

1. **Given** `createUniverseEntry(symbol, riskGroupId, isCef)` is called from the CSV import
   pipeline,
   **When** the function completes successfully,
   **Then** `prisma.universe.findUnique(entry.id)` returns a record where `volatility_long`
   and `volatility_short` are non-null (they may be `'insufficient-history'` if dividend
   history is unavailable, but the fields must have been written).

2. **Given** `getDistributions(symbol)` returns a non-empty `history` array,
   **When** `createUniverseEntry` runs,
   **Then** `recalculateUniverseVolatility` is called exactly once with `entry.id` and the
   full `history` array — matching the pattern used in `add-symbol.function.ts`.

3. **Given** `getDistributions(symbol)` returns an empty `history` array,
   **When** `createUniverseEntry` runs,
   **Then** `recalculateUniverseVolatility` is still called with the empty history so the
   volatility fields are set to `'insufficient-history'`, and the function does not throw.

4. **Given** the change is implemented with failing unit tests first (TDD),
   **When** `pnpm all` runs,
   **Then** all tests pass, including updated specs for `create-universe-entry.helper.ts`.

5. **Given** the updated `createUniverseEntry` now calls `getDistributions` before calling
   `fetchAndUpdatePriceData`,
   **When** it supplies the prefetched outcome as `prefetchedDistributionOutcome`,
   **Then** `getDistributions` is called at most once per symbol during import (no duplicate
   dividendhistory.net requests).

**Definition of Done:**

- [ ] Failing unit tests written before implementation for all three history scenarios (non-empty, empty, fetch error)
- [ ] `createUniverseEntry` calls `getDistributions`, then `recalculateUniverseVolatility(entry.id, history)`, then `fetchAndUpdatePriceData` with `prefetchedDistributionOutcome`
- [ ] No duplicate calls to `dividendhistory.net` per symbol
- [ ] Existing CSV import tests unchanged and passing
- [ ] `pnpm all` passes

---

### Story 92.2: E2E Regression Test — Volatility Present After CSV Import

**Slug:** `92-2-e2e-volatility-after-import`

As Dave,
I want a Playwright E2E test that imports a known CSV fixture and then asserts that the
imported symbol has non-null volatility fields in the universe API response,
So that a regression in the import volatility path is caught automatically before a release.

**Acceptance Criteria:**

1. **Given** a Playwright test fixture containing a single Fidelity CSV row for a known CEF
   ticker (e.g., PDI),
   **When** the fixture CSV is posted to `POST /api/import/fidelity`,
   **Then** a subsequent `GET /api/universe` response for that symbol includes
   `volatilityLong` and `volatilityShort` as non-null values.

2. **Given** the Playwright test suite,
   **When** `pnpm e2e:dms-material:chromium` and `pnpm e2e:dms-material:firefox` run,
   **Then** both pass, including the new import-volatility assertions.

3. **Given** the test environment may not have live internet access,
   **When** the E2E test runs,
   **Then** the `dividendhistory.net` fetch is intercepted (via `page.route` or
   `server.use`) and returns a fixture response, so the test is deterministic.

**Definition of Done:**

- [ ] Playwright test added or extended for the import-volatility scenario
- [ ] `dividendhistory.net` response mocked in the test fixture
- [ ] Both Chromium and Firefox pass
- [ ] `pnpm all` passes

---

## Epic 93: Grab Volatility During Update Fields

**Goal:** Ensure the batch "Update Fields" operation recalculates volatility for every active
universe symbol, so clicking the update button in the Settings panel keeps all `volatilityLong`
and `volatilityShort` columns current without requiring per-symbol manual edits.

---

### Story 93.1: Add Volatility Recalculation to the Batch Field Update Pipeline

**Slug:** `93-1-add-volatility-to-batch-update`

As a developer,
I want `processUniverse` in `apps/server/src/app/routes/settings/update/index.ts` to call
`recalculateUniverseVolatility(universe.id, history)` using the dividend history already
fetched during the same pass,
So that every "Update Fields" run refreshes both the price/distribution data and the
volatility classification for all universe symbols.

**Acceptance Criteria:**

1. **Given** `processUniverse(universe, logger)` is called for a symbol with existing dividend
   history,
   **When** `getDistributions(universe.symbol)` returns a `history` array with at least one
   row,
   **Then** `recalculateUniverseVolatility(universe.id, history)` is called during the same
   processing pass, and the `volatility_long`, `volatility_short`, and
   `volatility_calculated_at` fields are updated in the database.

2. **Given** `getDistributions` returns an empty `history` array for a symbol,
   **When** `processUniverse` runs for that symbol,
   **Then** `recalculateUniverseVolatility` is still called with the empty array so that
   `volatility_long` and `volatility_short` are set to `'insufficient-history'`.

3. **Given** the existing `checkForNewDistribution` helper only destructures `result` from
   `getDistributions`,
   **When** the fix is applied,
   **Then** `history` is also extracted and passed to `recalculateUniverseVolatility` — either
   by modifying `checkForNewDistribution` to return `{ result, history }` or by inlining the
   call (developer's choice, whichever is cleaner).

4. **Given** `recalculateUniverseVolatility` throws for a symbol,
   **When** the exception is caught in `processUniverse`,
   **Then** the symbol is marked as failed in the summary, and processing continues for the
   remaining symbols (same error-handling pattern as the existing price/distribution failure
   path).

5. **Given** the change is implemented with failing unit tests first (TDD),
   **When** `pnpm all` runs,
   **Then** all tests pass, including updated specs for `settings/update/index.ts`.

**Definition of Done:**

- [ ] Failing unit tests written before implementation
- [ ] `recalculateUniverseVolatility` called inside `processUniverse` using the `history` from `getDistributions`
- [ ] No additional `dividendhistory.net` request is made per symbol (reuse the same `getDistributions` call)
- [ ] `recalculateUniverseVolatility` failures counted in the update summary without aborting the batch
- [ ] `pnpm all` passes

---

## Epic 94: Return Symbol with divDeposit Data

**Goal:** Eliminate the client-side `buildUniverseMap()` lookup in the dividend-deposits
component service by having the `/api/div-deposits` endpoint return the resolved ticker symbol
alongside each deposit record.

---

### Story 94.1: Update divDeposit Server Endpoint to Return Symbol

**Slug:** `94-1-server-return-symbol-with-divdeposit`

As a developer,
I want the `DivDeposit` API response to include a `symbol: string | null` field resolved from
the linked universe record,
So that the client can render the deposit's ticker without performing a separate universe
store lookup.

**Acceptance Criteria:**

1. **Given** the `POST /api/div-deposits/` endpoint is called with an array of deposit IDs,
   **When** the server queries the database,
   **Then** each returned `DivDeposit` object includes a `symbol` field resolved from
   `divDeposits.universe.symbol` (or `null` if `universeId` is `null`).

2. **Given** the Prisma query currently uses `prisma.divDeposits.findMany({ where: { id: { in: ids } } })`,
   **When** the fix is applied,
   **Then** the query includes `include: { universe: { select: { symbol: true } } }` so
   the join is performed in a single database round-trip.

3. **Given** the `handleAddDivDepositRoute` (`POST /api/div-deposits/add`) creates a new
   deposit,
   **When** the response is sent,
   **Then** the returned `DivDeposit` object also includes the resolved `symbol` field.

4. **Given** the `handleUpdateDivDepositRoute` (`PUT /api/div-deposits/`) updates a deposit,
   **When** the response is sent,
   **Then** the returned `DivDeposit` objects also include the resolved `symbol` field.

5. **Given** the server-side `DivDeposit` interface in `div-deposits.interface.ts`,
   **When** the fix is applied,
   **Then** the interface includes `symbol: string | null` alongside the existing fields.

6. **Given** the change is implemented with failing unit tests first (TDD),
   **When** `pnpm all` runs,
   **Then** all tests pass, including updated specs for `div-deposits/index.ts`.

**Definition of Done:**

- [ ] Failing unit tests written before implementation
- [ ] `prisma.divDeposits.findMany`, `create`, and `update` calls all use `include: { universe: { select: { symbol: true } } }`
- [ ] `mapDivDepositToResponse` maps `u.universe?.symbol ?? null` to `symbol`
- [ ] Server `DivDeposit` interface updated to include `symbol: string | null`
- [ ] `pnpm all` passes

---

### Story 94.2: Remove Universe Lookup Map from DivDeposit Client Component

**Slug:** `94-2-client-remove-universe-map-divdeposit`

As a developer,
I want `dividend-deposits-component.service.ts` to read `d.symbol` directly from each
`DivDeposit` row instead of building a `universeMap` and performing a `universeMap.get(d.universeId)` lookup,
So that the dividend-deposits display does not depend on the universe store being fully loaded.

**Acceptance Criteria:**

1. **Given** the client-side `DivDeposit` interface in
   `apps/dms-material/src/app/store/div-deposits/div-deposit.interface.ts`,
   **When** the fix is applied,
   **Then** the interface includes `symbol: string | null` and the server contract is satisfied.

2. **Given** `buildLoadedDividendRow` in `dividend-deposits-component.service.ts` currently
   uses `universeMap.get(d.universeId)?.symbol ?? ''`,
   **When** the fix is applied,
   **Then** it uses `d.symbol ?? ''` directly, with no reference to `universeMap`.

3. **Given** the `dividends` computed signal currently calls `buildUniverseMap()`,
   **When** the fix is applied,
   **Then** `buildUniverseMap()` is no longer called from this file.

4. **Given** the deposit table renders the symbol column,
   **When** a deposit row is displayed,
   **Then** the ticker symbol is visible and matches the symbol returned by the API (verified
   via Playwright MCP server).

5. **Given** the change is implemented with failing unit tests first (TDD),
   **When** `pnpm all` runs,
   **Then** all tests pass, including updated specs for
   `dividend-deposits-component.service.ts`.

**Definition of Done:**

- [ ] Failing unit tests written before implementation
- [ ] Client `DivDeposit` interface updated to include `symbol: string | null`
- [ ] `buildLoadedDividendRow` uses `d.symbol ?? ''` not `universeMap.get(...)`
- [ ] `buildUniverseMap()` no longer imported or called in `dividend-deposits-component.service.ts`
- [ ] Playwright MCP server confirms symbol column renders correctly
- [ ] `pnpm all` passes

---

## Epic 95: Return Symbol with Open Positions Data

**Goal:** Remove the universe-map dependency from the open-positions component service by
enforcing that `trade.symbol` is always present in the `Trade` interface and using it directly
in position calculations, eliminating the `buildUniverseMap()` call from the open-positions
data pipeline.

---

### Story 95.1: Enforce Symbol as Required in the Trade Interface

**Slug:** `95-1-enforce-symbol-required-in-trade`

As a developer,
I want `symbol` to be a required (non-optional) field in both the server and client `Trade`
interfaces, and the SmartNgRX `/api/trades/` POST endpoint to always return a non-null symbol,
So that downstream components can safely access `trade.symbol` without optional chaining or
fallback lookups.

**Acceptance Criteria:**

1. **Given** the server `Trade` interface in `apps/server/src/app/routes/trades/index.ts`
   currently has `symbol?: string`,
   **When** the fix is applied,
   **Then** the field is changed to `symbol: string` and the `mapTradeToResponse` function
   maps `trade.universe?.symbol ?? ''` (or an equivalent safe default) to ensure it is always
   a string.

2. **Given** the client `Trade` interface in
   `apps/dms-material/src/app/store/trades/trade.interface.ts` currently has `symbol?: string`,
   **When** the fix is applied,
   **Then** the field is changed to `symbol: string`.

3. **Given** the Prisma query in `handleGetTradesRoute` already includes
   `universe: { select: { symbol: true } }`,
   **When** a trade is retrieved for a symbol that no longer exists in the universe (soft-deleted
   or orphaned),
   **Then** `symbol` falls back to an empty string `''` rather than throwing or omitting the field.

4. **Given** the change is implemented with failing unit tests first (TDD),
   **When** `pnpm all` runs,
   **Then** all tests pass, including updated specs for `routes/trades/index.ts`.

**Definition of Done:**

- [ ] Failing unit tests written before implementation
- [ ] `symbol?: string` changed to `symbol: string` in both server and client Trade interfaces
- [ ] `mapTradeToResponse` always produces a string value for `symbol`
- [ ] `pnpm all` passes

---

### Story 95.2: Remove Universe Lookup Map from Open Positions Client Component

**Slug:** `95-2-client-remove-universe-map-open-positions`

As a developer,
I want `open-positions-component.service.ts` to use `trade.symbol` directly when building
`OpenPosition` objects instead of calling `buildUniverseMap()` and performing
`universeMap.get(trade.universeId)`,
So that the open-positions display is independent of the universe store being loaded.

**Acceptance Criteria:**

1. **Given** `selectOpenPositions` in `open-positions-component.service.ts` currently calls
   `this.universeMap()` and uses the result to look up `universe = universeMap.get(trade.universeId)`,
   **When** the fix is applied,
   **Then** the symbol is read directly from `trade.symbol` without any map lookup.

2. **Given** the `transformTradeToPosition(trade, universe)` helper currently accepts a
   `Universe` object and uses `universe.symbol`,
   **When** the fix is applied,
   **Then** the helper signature is updated to accept `symbol: string` directly (or the
   symbol is inlined), removing the `Universe` parameter dependency.

3. **Given** the `partialOpenPosition(trade)` fallback is currently triggered when
   `universe === undefined`,
   **When** the fix is applied,
   **Then** the fallback is simplified or removed — `trade.symbol` is always present after
   Story 95.1, so `partialOpenPosition` is no longer needed for symbol resolution.

4. **Given** `private universeMap()` helper and its `buildUniverseMap()` call exist in
   `open-positions-component.service.ts`,
   **When** the fix is applied,
   **Then** both are removed.

5. **Given** the open-positions table renders symbol and calculated columns (Current Value,
   Unrealized Gain),
   **When** a position row is displayed,
   **Then** the symbol and all calculated values are correct (verified via Playwright MCP
   server).

6. **Given** the change is implemented with failing unit tests first (TDD),
   **When** `pnpm all` runs,
   **Then** all tests pass, including updated specs for `open-positions-component.service.ts`.

**Definition of Done:**

- [ ] Failing unit tests written before implementation
- [ ] `selectOpenPositions` uses `trade.symbol` directly, no map lookup
- [ ] `buildUniverseMap()` not imported or called in `open-positions-component.service.ts`
- [ ] `private universeMap()` helper removed
- [ ] Playwright MCP server confirms all open-positions columns render correctly
- [ ] `pnpm all` passes

---

## Epic 96: Return Symbol with Sold Positions Data

**Goal:** Remove the universe-map dependency from the sold-positions component service
(mirroring Epic 95 for the closed/sold positions view), and then delete the `buildUniverseMap`
function entirely once it has no remaining callers.

---

### Story 96.1: Remove Universe Lookup Map from Sold Positions Client Component

**Slug:** `96-1-client-remove-universe-map-sold-positions`

As a developer,
I want `sold-positions-component.service.ts` to use `trade.symbol` directly when building
`ClosedPosition` objects instead of calling `buildUniverseMap()` and performing
`universeMap.get(trade.universeId)`,
So that the sold-positions display is independent of the universe store being loaded, and the
`buildUniverseMap` function has no remaining callers.

**Acceptance Criteria:**

1. **Given** `selectSoldPositions` in `sold-positions-component.service.ts` currently calls
   `buildUniverseMap()` and uses `universeMap.get(trade.universeId)`,
   **When** the fix is applied,
   **Then** the symbol is read directly from `trade.symbol` (made non-optional in Story 95.1).

2. **Given** `buildFullClosedPosition(trade, universe)` currently accepts a `Universe` object,
   **When** the fix is applied,
   **Then** the helper is updated to accept the relevant fields directly (or `symbol: string`)
   rather than a full `Universe` object.

3. **Given** `buildPartialClosedPosition(trade)` currently exists as a fallback for when the
   universe lookup returns `undefined`,
   **When** the fix is applied,
   **Then** the fallback is simplified or removed — `trade.symbol` is always present, so
   partial positions due to missing universe data are no longer needed.

4. **Given** `buildUniverseMap()` is called at the top of `selectSoldPositions`,
   **When** the fix is applied,
   **Then** `buildUniverseMap()` is no longer imported or called in this file.

5. **Given** the sold-positions table renders symbol, buy price, sell price, profit, and
   percent-gain columns,
   **When** a sold position row is displayed,
   **Then** all column values are correct (verified via Playwright MCP server).

6. **Given** the change is implemented with failing unit tests first (TDD),
   **When** `pnpm all` runs,
   **Then** all tests pass, including updated specs for `sold-positions-component.service.ts`.

**Definition of Done:**

- [ ] Failing unit tests written before implementation
- [ ] `selectSoldPositions` uses `trade.symbol` directly, no map lookup
- [ ] `buildUniverseMap()` not imported or called in `sold-positions-component.service.ts`
- [ ] `buildPartialClosedPosition` / `buildFullClosedPosition` universe-parameter dependency removed
- [ ] Playwright MCP server confirms all sold-positions columns render correctly
- [ ] `pnpm all` passes

---

### Story 96.2: Delete buildUniverseMap and Remove All Remaining References

**Slug:** `96-2-delete-build-universe-map`

As a developer,
I want to delete `apps/dms-material/src/app/shared/build-universe-map.function.ts` and
remove all remaining imports or references to `buildUniverseMap` in the codebase,
So that the dead-code function and its dependency on the universe store are permanently
eliminated.

**Acceptance Criteria:**

1. **Given** Epics 94, 95, and 96.1 are complete (i.e., `buildUniverseMap` is no longer
   called from `dividend-deposits-component.service.ts`,
   `open-positions-component.service.ts`, or `sold-positions-component.service.ts`),
   **When** a repository-wide search for `buildUniverseMap` is performed,
   **Then** the only remaining occurrence is the function definition file itself.

2. **Given** `build-universe-map.function.ts` is deleted,
   **When** `pnpm all` runs,
   **Then** there are no TypeScript compilation errors referencing the deleted file.

3. **Given** the `buildUniverseMap` spec file (if one exists) was testing the function,
   **When** the source file is deleted,
   **Then** the spec file is also deleted.

4. **Given** the deletion is complete,
   **When** the Playwright E2E suite runs (`pnpm e2e:dms-material:chromium` and
   `pnpm e2e:dms-material:firefox`),
   **Then** all tests pass — confirming that dividend-deposits, open-positions, and
   sold-positions panels still display correctly without the universe map.

**Definition of Done:**

- [ ] `build-universe-map.function.ts` deleted
- [ ] Companion spec file deleted (if present)
- [ ] Zero TypeScript errors after deletion
- [ ] No remaining `import.*buildUniverseMap` in the codebase
- [ ] E2E suite passes on both Chromium and Firefox
- [ ] `pnpm all` passes
