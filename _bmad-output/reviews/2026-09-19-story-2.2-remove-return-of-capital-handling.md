# Code Review Log — Story 2.2: Remove Return of Capital handling

**Date:** 2026-09-19T02:31:58Z
**Story file:** `_bmad-output/implementation-artifacts/stories/epic-2/2.2-remove-return-of-capital-handling.md`
**Paired unit-test story:** `2.1-unit-tests-for-removing-return-of-capital.md` (status: done — not audited as a second spec document)
**Diff scope:** 3 files (`scripts/seed-postgres.ts`, `scripts/seed-postgres.spec.ts`, `docs/data-models.md`)

---

## Findings

### [spec] AC #3 is self-contradictory with Task 1.1 and AC #5 — zero matches unachievable as written

**AC violated:** #3
**Evidence:** Case-insensitive grep for "return of capital" outside `_bmad-output/` returns 3 files: `scripts/seed-postgres.spec.ts` (absence assertions protected by Task 1.1's explicit prohibition), `apps/server/src/app/routes/top/get-top-div-deposit-types.function.spec.ts` (canonical-seed guard tests protected by AC #5 / "do not touch"), and `.github/epic-descriptions/epics-2026-07-14.md` (planning artifact). The dev agent's judgment call is internally consistent — the criterion as literally worded cannot be satisfied without violating Task 1.1 or modifying planning artifacts — but no spec amendment or waiver was recorded in the story file.
**Resolution needed:** Amend AC #3 to exclude test assertions and epic prose (e.g., "zero matches outside `_bmad-output/`, `.github/epic-descriptions/`, and test assertion strings"), or record an explicit waiver with justification.

### [spec] AC #4 cannot be satisfied in this environment — false premise about tsx availability

**AC violated:** #4
**Evidence:** `tsx scripts/seed-postgres.ts` is impossible: (a) no `tsx` binary exists in `node_modules/.bin/`; (b) the repo pins Prisma 7 which requires a driver adapter for bare `new PrismaClient()` — the seed script predates that requirement. This is a pre-existing environmental issue, not caused by this diff. The seeding contract is covered by mocked unit tests (`scripts/seed-postgres.spec.ts`, 3/3 passing) instead.
**Resolution needed:** Amend AC #4 to "seeding contract verified via unit tests" (reflecting actual verification method), or fix the seed script's Prisma client instantiation as a separate story.

### [patch] Out-of-scope tracked change: `scripts/project.json` modified; untracked `scripts/coverage/` directory

**AC violated:** None directly — contradicts Dev Notes ("removal surface is exactly 2 lines", "no new files created") and the File List (3 files only).
**Evidence:** `git diff HEAD -- scripts/project.json` shows an added `--ignore-pattern 'coverage'` flag to the eslint command. An untracked `scripts/coverage/` directory contains test coverage output artifacts (HTML, JSON, CSS, JS). Neither appears in the story's File List nor is mapped to any task.
**Resolution needed:** Revert `scripts/project.json` to its HEAD state (the eslint ignore-pattern change belongs in a separate config story), and either add `scripts/coverage/` to `.gitignore` or delete it before committing.

### [defer] No cleanup path for pre-existing database rows — seeder only upserts, never deletes

**AC violated:** None (pre-existing design characteristic)
**Evidence:** Removing `{ name: 'Return of Capital' }` from the `divDepositTypes` array means re-running the seed will upsert Dividend/Interest/Capital Gains but never delete an existing "Return of Capital" row. Any database seeded before this change retains the stale row indefinitely. No migration or reconciliation logic exists in the diff. The story explicitly states "no migration needed."
**Why deferred:** This is a pre-existing limitation of the upsert-only seeder pattern, not introduced by this diff. The unit tests verify the seeding contract for fresh databases only. If live environments need reconciliation, a one-time `deleteMany` or migration should be addressed in a separate story.

### [defer] Doc example mismatch: "Capital Gain" (singular) vs seed value "Capital Gains" (plural)

**AC violated:** None (pre-existing inaccuracy)
**Evidence:** The edited comment at `docs/data-models.md:222` reads `// e.g. "Dividend", "Capital Gain"` but the seeder's actual value is `'Capital Gains'` (plural). This mismatch predates this diff — the line previously read `"Dividend", "Capital Gain", "Return of Capital"`.
**Why deferred:** Pre-existing documentation inaccuracy, not introduced by this change. Fix in a future doc cleanup pass.

---

## Verdict: needs-human

Two spec findings require human decision before the story can be marked complete:
1. AC #3 wording must be amended or waived (self-contradictory with Task 1.1).
2. AC #4 premise is false in this environment (tsx/Prisma 7 incompatibility).

One patch finding (`scripts/project.json` + `scripts/coverage/`) should be resolved before committing — revert the config change and handle the coverage directory.

Two findings are deferred to future work (DB row cleanup, doc singular/plural fix).
